# Tauri→Flutter parity table — 2026-09-14 audit (Task 12, arxa-studio closeout)

Every §a box of `arxa-studio/docs/plans/mobile-flutter-migration-spec.md`, mapped to
current Flutter source plus automated or physical evidence. Audited at arxa
closeout commit `27878bcb` (post Step-1 hygiene), pinned SDK fvm 3.44.9.

Automated evidence for this audit: `fvm flutter analyze` — No issues found;
`fvm flutter test` — +104 all passed; `kit/studio_transport` `fvm flutter test` — +7
all passed; `cargo test --lib` in `kit/studio_transport/rust` — **11 passed, 0 failed**.
Physical/simulator evidence: `evidence/lens-smoke-2026-08-29/` (Task 1 capture).

| # | Legacy box (Tauri anchor) | Flutter source | Evidence | Verdict |
|---|---|---|---|---|
| 1 | Pairing screen states mirror `ConnectionStatus` (`mobile/src/main.js`) | `lib/services/transport_service.dart:6` enum `ArxaConnectionState {notPaired, scanning, pairing, connecting, connected}`; pairing_shell views | `test/viewmodels/pairing_connecting_viewmodel_test.dart`, `pairing_scan_viewmodel_test.dart` (+104 suite); `lens-smoke-2026-08-29/pairing-scan.png`, `pairing-connecting.png` | ✅ |
| 2 | QR scan — `tauri-plugin-barcode-scanner 2.4` (`main.js:71-76`) | `lib/ui/views/pairing_shell/pairing_scan/` — `mobile_scanner ^7` (MLKit/AVFoundation; qr_code_scanner unmaintained) | `pairing_scan_viewmodel_test.dart`; `pairing-scan.png` (live camera scan shot) | ✅ |
| 3 | Manual ticket paste (`mobile-pairing-transport.md:36-37`) | `pairing_scan_view.dart:47` `ArxaKitNativeTextField` paste fallback | `pairing_scan_viewmodel_test.dart:22-34` (submitTicket happy path + double-submit guard) | ✅ |
| 4 | Flow contract `begin_pairing`/`connection_status` 2s poll (`main.js:2-5,41-45`) | `lib/services/transport_service.dart:19` pins the invoke surface; `iroh_transport_service.dart` implements | `test/services/transport_service_test.dart`; `kit/studio_transport/test/mock_transport_test.dart` (+7) | ✅ |
| 5 | iroh client — ALPN `arxa/studio/0`, AUTH_TIMEOUT 15s, RECONNECT_BACKOFF 3s, FRESH 2 / RECONNECT 5 dials (`connection.rs:44-50`) | `kit/studio_transport/rust/src/transport.rs:29-34` — byte-identical constants; reconnect loop `:318-380` (`max_dial_attempts = RECONNECT_DIAL_ATTEMPTS:337`, backoff `:375`) | `cargo test --lib` 11/11 (ticket parse roundtrip, auth frame, push frame) | ✅ |
| 6 | Loopback HTTP proxy on random port, `studio_url = 127.0.0.1:<port>` (`connection.rs:307`) | `transport.rs:449` `TcpListener::bind(("127.0.0.1", 0))`; UA-pinned webview `lib/ui/views/studio_shell/studio_session/studio_session_viewmodel.dart:2,14` | `lens-smoke-2026-08-29/e2e-roundtrip.txt` + `e2e-studio-session-connected.png` (real pairhost/iroh route) | ✅ |
| 7 | Persistence/reconnect — store NodeId + session token, skip QR (`transport.md:25-28`) | `iroh_transport_service.dart:89,146,173` — payload persisted (shared_preferences), `resume()` cold-start re-dial | `test/app_data_sync_boot_test.dart`; e2e reconnect case `integration_test/approvals_e2e_test.dart:312` (CELLULAR tunnel re-establish) | ✅ |
| 8 | Revocation = AUTH-stream close only; PUSH-stream close never revokes (`connection.rs:304-305`) | `transport.rs:14` contract; `establish()` AUTH refusal → `LinkState::Disconnected` (no redial); `register_push_over_tunnel` `:382` failures logged/swallowed, never state-changing | `cargo test --lib` 11/11 | ✅ |
| 9 | Push token relay `set_push_token` + `PUSH <sess> <platform> <token>\n` → `OK\n`, best-effort (`connection.rs:347-370`) | `transport.rs:88,162,351,382-390` (frame + re-send on reconnect); Dart `lib/services/push_token_service.dart:27` → `setPushToken` rail + cairn register | `test/services/push_token_service_test.dart`, `apns_notifications_backend_test.dart`; `pairing-push-permission.png`; e2e real-APNs-token case `approvals_e2e_test.dart:316+` | ✅ |
| 10 | Device name via `gethostname` in AUTH frame (`connection.rs:399`) | `transport.rs:82-83` `auth_frame(token, device_name)`; `iroh_transport_service.dart` `_deviceName` seam | `cargo test --lib` (auth_frame test) | ✅ |
| 11 | Identity/config — productName Arxa Studio, 0.1.0, id `solutions.arxadigital.arxa.mobile`, team `43GNRCGQXQ`, UA ArxaShell/0.1 | pubspec `version: 0.1.0`; `android/app/build.gradle.kts:34` applicationId; `ios/Runner.xcodeproj/project.pbxproj:470,478` team+bundle; `studio_session_viewmodel.dart:14` UA `Mozilla/5.0 (Mobile) ArxaShell/0.1` | CI lane asserts ids (Task 12 `scripts/check.sh` mobile-signing area); `flutter analyze` | ✅ |
| 12 | Build facts — `tauri android init` w/ NDK 29.0.14206865, iOS project generated (grill) | Flutter: android/ + ios/ projects committed; NDK via `flutter.ndkVersion` (tool-managed — accepted deviation: the Flutter Gradle plugin owns the NDK pin, not the repo); SPM plugin copies under `build/ios/SourcePackages` | android/ios projects build (Step-1 hygiene run green); release-shaped AAB smoke (disposable keystore) + iOS archive doctor validation = Part B gates, PENDING at table commit | ✅ (smoke pending Part B) |

## Residuals this audit closes (Task-12 scope, not parity)

- Android release signing (box-adjacent AXS-011): Step 4 of this task.
- CI frame + release automation (AXS-011/AXS-043): Steps 6–8.
- Physical-device gates (AXS-018): Step 9 doc + Task 16 execution.

Legacy Tauri scaffold (`arxa/mobile/`) retained per Step 10 (AXS-031) — removal is a separate
reviewed cleanup after the first accepted iOS and Android releases.
