name: text_field_m3e
description: "Material 3 Expressive Text Field with focus shape-morph, sizes, shapes, error states, and a11y. First-party stacked_kit code — see README.md."
version: 0.1.0
publish_to: none
homepage: https://github.com/unfazed-dev/stacked_kit

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  m3e_design: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
