# fab_m3e — vendored fork playbook

**Version:** 0.1.1 (fork) · **Upstream:** `EmilyMoonstone/material_3_expressive` · **Status:** vendored fork, pinned by root `dependency_overrides`

**Role (one line):** Material 3 Expressive Floating Action Button, Extended FAB, and FAB Menu — forked so the menu reveals items with a staggered, FAB-anchored spring instead of a single centre-origin scale+fade.

<Callout id="fab-fork-banner" tone="warning">

Vendored fork of upstream `fab_m3e` `0.1.1`. The patched source lives in this repo at `packages/fab_m3e/lib/src/fab_menu_m3e.dart`. The patch is activated by the root `pubspec.yaml` `dependency_overrides` `path:` entry — remove that entry and `flutter pub get` silently reverts to upstream.

</Callout>

## Contents

0. Fork banner
1. Overview & role
2. Glossary
3. Architecture — fork contract + FileTree
4. Public API reference
5. Usage playbook
5b. Wiring
6. Integration map
7. Features appendix
8. Examples
9. Gotchas + re-sync procedure
10. Decisions / ADRs
11. Changelog & status
12. Open questions

### Overview & role

`fab_m3e` is a leaf Material 3 Expressive UI widget package. Hexagonal / domain-layer concerns are N/A — it has no data, service, or routing surface. It only renders widgets and consumes `m3e_design` tokens.

This package is one of four vendored forks of `EmilyMoonstone/material_3_expressive` pinned in this repo (alongside `navigation_rail_m3e`, `split_button_m3e`, `toolbar_m3e`). Each fork exists because a specific upstream behaviour was wrong for this product; the patch is small and surgical.

### What this is

A vendored fork of upstream `fab_m3e` `0.1.1`. The fork is pulled in transitively via the `m3e_collection` umbrella package, and activated by a `path:` `dependency_overrides` entry in the root `pubspec.yaml`. The patched file is `packages/fab_m3e/lib/src/fab_menu_m3e.dart`. The patch marker in source is `// Patched (vendored fork)`.

### Glossary

- **Fork** — a vendored copy of an upstream package, edited in-tree, pinned by a `path:` override.
- **Override** — the `dependency_overrides:` block in the root `pubspec.yaml` that forces `pub` to resolve this package from a local folder instead of pub.dev.
- **Patch marker** — an inline comment tag (`// Patched (vendored fork)`, `// Fork patch:`, or `// PATCH`) that flags every block of code changed from upstream. Audit with `grep -rEn 'FORK|fork patch|PATCH|vendored' packages/fab_m3e/lib`.
- **Re-sync** — the procedure for re-applying this fork's patches onto a newer upstream version (see §9).
- **M3E** — Material 3 Expressive, the motion/shape system this widget implements.

### Architecture — the fork contract

The fork contract has two halves: **WHY** the patch exists (the root `pubspec.yaml` override comment) and **HOW** it is implemented (the inline `// Patched (vendored fork)` / `// Fork patch:` / `// PATCH` markers in `lib/src/`).

<Diagram id="fab-arch-diagram" caption="fab_m3e fork wiring: root override → umbrella → fork package.">

```html
<div class="diagram-panel" data-rough>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">root pubspec.yaml</div>
    <div class="diagram-node">dependency_overrides</div>
  </div>
  <div class="diagram-arrow">↓ activates</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">m3e_collection</div>
    <div class="diagram-node">umbrella re-export</div>
  </div>
  <div class="diagram-arrow">↓ resolves to</div>
  <div class="diagram-box diagram-accent" data-rough>
    <div class="diagram-pill">fab_m3e (fork)</div>
    <div class="diagram-node">lib/src/fab_menu_m3e.dart</div>
  </div>
  <div class="diagram-arrow">↓ consumed by</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">stacked_kit</div>
    <div class="diagram-node">Kit* wrappers</div>
  </div>
</div>
```

```css
.diagram-panel { display: flex; flex-direction: column; gap: 8px; }
.diagram-arrow { text-align: center; padding: 2px 0; opacity: .7; }
```

</Diagram>

<FileTree id="fab-file-tree" title="fab_m3e — files that matter for the fork" entries={[
  { path: "packages/fab_m3e/lib/fab_m3e.dart", change: "modified", note: "Barrel; re-exports src/fab_theme_m3e.dart show FabTokensAdapter." },
  { path: "packages/fab_m3e/lib/src/fab_menu_m3e.dart", change: "modified", note: "PATCHED — staggered FAB-anchored spring reveal. Single // Patched (vendored fork) marker block." },
  { path: "packages/fab_m3e/lib/src/extended_fab_m3e.dart", change: "modified", note: "Unpatched; imported by FabMenuM3E to render each item as ExtendedFabM3E." },
  { path: "packages/fab_m3e/lib/src/fab_theme_m3e.dart", change: "modified", note: "FabTokensAdapter; unpatched." },
  { path: "packages/fab_m3e/lib/src/enums.dart", change: "modified", note: "FabM3EKind/Size/Density + FabMenuDirection enum." },
  { path: "packages/fab_m3e/test/fab_m3e_test.dart", change: "modified", note: "Test/ exists." },
  { path: "packages/fab_m3e/CHANGELOG.md", change: "modified", note: "STALE — only 0.1.0 'Changelog initialized.'; 0.1.1 needs backfill." },
  { path: "packages/fab_m3e/pubspec.yaml", change: "modified", note: "issue_tracker has EmilyMonestone (sic) typo." },
]} />

### The fork contract (shared)

Every vendored fork in this repo carries the same contract:

- The **WHY** is documented in the root `pubspec.yaml` `dependency_overrides` comment block above each `path:` entry. Read it before editing.
- The **HOW** is flagged in source with `// Patched (vendored fork)`, `// Fork patch:`, or `// PATCH`. Every changed block has a marker.
- The **audit command** is `grep -rEn 'FORK|fork patch|PATCH|vendored' packages/<name>/lib` — re-run after every edit and before every re-sync.
- The **doc-gap checklist** (every fork carries this debt): add a `> Vendored fork of upstream <name> <version>` banner to the README; backfill the CHANGELOG; fix the `EmilyMonestone` → `EmilyMoonstone` typo in `issue_tracker` (present in every fork's `pubspec.yaml`).
- The **test contract**: every fork SHOULD ship a `test/`. `navigation_rail_m3e` currently has NONE (highest debt); the other three forks do ship one.

### Public API reference

The barrel `lib/fab_m3e.dart` exports five symbols. The fork only changes the internals of `FabMenuM3E`; the public signatures are unchanged from upstream.

<TabsBlock id="fab-api-tabs" tabs={[
  {
    id: "fab-api-menu",
    label: "FabMenuM3E",
    blocks: [
      {
        id: "fab-api-menu-code",
        type: "annotated-code",
        data: {
          filename: "packages/fab_m3e/lib/src/fab_menu_m3e.dart",
          language: "dart",
          code: "class FabMenuM3E extends StatefulWidget {\n  const FabMenuM3E({\n    required this.primaryFab,\n    required this.items,\n    this.direction = FabMenuDirection.up,\n    this.spacing,\n    this.overlay = true,\n    this.alignment = Alignment.bottomRight,\n    this.popOnItemTap = true,\n    this.controller,\n    this.heroTag,\n  });\n\n  final Widget primaryFab;\n  final List<FabMenuItem> items;\n  final FabMenuDirection direction;\n  final FabMenuController? controller;\n  // ... see source for the rest\n}\n\nclass FabMenuItem {\n  final Widget icon;\n  final Widget label;\n  final VoidCallback onPressed;\n  final String? semanticLabel;\n}\n\nclass FabMenuController extends ChangeNotifier {\n  bool get isOpen;\n  void open();\n  void close();\n  void toggle();\n}",
          annotations: [
            { lines: "1-13", label: "FabMenuM3E", note: "StatefulWidget. The fork only changes the State's reveal animation; constructor signature is identical to upstream." },
            { lines: "15-17", label: "FabMenuDirection", note: "Re-exported from enums.dart. Drives _originFor(direction) so each item scales from the corner facing the FAB." },
            { lines: "25-30", label: "FabMenuItem", note: "Plain model: icon, label, onPressed, optional semanticLabel." },
            { lines: "32-37", label: "FabMenuController", note: "External controller hook; open/close/toggle + isOpen. Optional — fork creates an internal one if omitted." },
          ],
        },
      },
    ],
  },
  {
    id: "fab-api-fab",
    label: "FabM3E / ExtendedFabM3E",
    blocks: [
      {
        id: "fab-api-fab-code",
        type: "rich-text",
        data: {
          markdown: "`FabM3E` and `ExtendedFabM3E` (in `src/fab_m3e.dart` and `src/extended_fab_m3e.dart`) are **unchanged from upstream**. They take `kind` (`FabM3EKind`), `size` (`FabM3ESize`), `density` (`FabM3EDensity`), `onPressed`, and standard M3E token-driven styling. `FabMenuM3E` builds each menu item as an `ExtendedFabM3E` with `kind: FabM3EKind.surface`.",
        },
      },
    ],
  },
  {
    id: "fab-api-tokens",
    label: "FabTokensAdapter",
    blocks: [
      {
        id: "fab-api-tokens-code",
        type: "rich-text",
        data: {
          markdown: "`FabTokensAdapter` (in `src/fab_theme_m3e.dart`, exported via `show`) bridges `m3e_design` tokens into FAB colors/shapes. **Unpatched.** Re-exported from the barrel as `export 'src/fab_theme_m3e.dart' show FabTokensAdapter;`.",
        },
      },
    ],
  },
]} />

### Usage playbook

<AnnotatedCode id="fab-usage-menu" filename="example/fab_menu_usage.dart" language="dart" code={"import 'package:m3e_collection/m3e_collection.dart';\n\nFabMenuM3E(\n  primaryFab: FabM3E(\n    icon: const Icon(Icons.add),\n    onPressed: () {},\n  ),\n  direction: FabMenuDirection.up,\n  items: [\n    FabMenuItem(icon: const Icon(Icons.note), label: const Text('Note'), onPressed: () {}),\n    FabMenuItem(icon: const Icon(Icons.task), label: const Text('Task'), onPressed: () {}),\n  ],\n)"} annotations={[
  { lines: "1", note: "Hosts import via the umbrella m3e_collection, not the fork directly (see 5b)." },
  { lines: "5", label: "direction", note: "Drives _originFor(): up=bottomRight, down=topRight, left=centerRight, right=centerLeft. Each item scales from that corner of its own box." },
  { lines: "7-10", label: "items", note: "Reveal order = list order; the item nearest the FAB leads the stagger." },
]} />

## 5b. Wiring — how a host actually gets this fork

<AnnotatedCode id="fab-wiring-override" filename="pubspec.yaml (root)" language="yaml" code={"# Vendored fork of fab_m3e 0.1.1 (pulled in transitively via m3e_collection).\n# Patched so the FAB Menu reveals items with a staggered, FAB-anchored spring\n# (M3E spec) instead of one center-origin scale+fade, and never draws a scrim.\n# Source of the patch: packages/fab_m3e/lib/src/fab_menu_m3e.dart.\ndependency_overrides:\n  fab_m3e:\n    path: packages/fab_m3e"} annotations={[
  { lines: "1-4", label: "WHY", note: "Root pubspec.yaml override comment. This is the auditable reason for the fork; edit it whenever the patch changes." },
  { lines: "5-7", label: "HOW", note: "path: override activates the fork. m3e_collection still re-exports fab_m3e by name; the override redirects that name to the local folder." },
]} />

<Callout id="fab-wiring-callout" tone="info">

Hosts almost never import `package:fab_m3e/...` directly. The fork is pulled in **transitively** via `m3e_collection` (the umbrella that re-exports every M3E widget), and most apps reach it through `stacked_kit`'s `Kit*` wrappers. The root `dependency_overrides` `path:` entry is the only thing that activates the patch — `m3e_collection`'s own version constraint is unchanged.

</Callout>

### Integration map

<Diagram id="fab-integration-map" caption="Apps import m3e_collection; the root override redirects fab_m3e to the local fork.">

```html
<div class="diagram-panel" data-rough>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">App (showcase / host)</div>
  </div>
  <div class="diagram-arrow">↓ imports</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">m3e_collection</div>
    <div class="diagram-node">umbrella</div>
  </div>
  <div class="diagram-arrow">↓ re-exports</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">stacked_kit</div>
    <div class="diagram-node">Kit* wrappers</div>
  </div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">root pubspec.yaml</div>
    <div class="diagram-node">dependency_overrides: fab_m3e path:packages/fab_m3e</div>
  </div>
  <div class="diagram-arrow">↓ redirects</div>
  <div class="diagram-box diagram-accent" data-rough>
    <div class="diagram-pill">packages/fab_m3e</div>
    <div class="diagram-node">this fork, not pub.dev</div>
  </div>
</div>
```

```css
.diagram-panel { display: flex; flex-direction: column; gap: 8px; }
.diagram-arrow { text-align: center; padding: 2px 0; opacity: .7; }
```

</Diagram>

### Features appendix

<Checklist id="fab-features" items={[
  { id: "feat-core-fab", label: "FabM3E (small/medium/large)", note: "Unchanged from upstream." },
  { id: "feat-extended-fab", label: "ExtendedFabM3E (label + icon)", note: "Unchanged; reused as the menu item body." },
  { id: "feat-menu-stagger", label: "FabMenuM3E staggered spring reveal (fork)", checked: true, note: "Each item scales from the corner facing the FAB via _originFor(direction)." },
  { id: "feat-menu-noscrim", label: "FabMenuM3E never draws a scrim (fork)", checked: true, note: "M3E spec; root pubspec override comment calls this out explicitly." },
  { id: "feat-controller", label: "FabMenuController open/close/toggle" },
  { id: "feat-tokens", label: "FabTokensAdapter bridges m3e_design tokens" },
]} />

### Examples

<Code id="fab-example-controller" filename="example/controlled_menu.dart" language="dart" caption="Drive the menu from an external controller (e.g. from a ViewModel)." code={"final c = FabMenuController();\n// ...\nFabMenuM3E(\n  controller: c,\n  primaryFab: FabM3E(icon: const Icon(Icons.add), onPressed: c.toggle),\n  items: [/* ... */],\n);"} />

### Gotchas + re-sync procedure

<Callout id="fab-gotcha-leak" tone="risk">

The fork uses `Interval.transform` on the raw controller value **instead** of allocating a `CurvedAnimation` per item per build. Upstream's per-item `CurvedAnimation` allocation leaked in Flutter 3.44. Do not "simplify" the loop back to `CurvedAnimation` — that reintroduces the leak.

</Callout>

<Callout id="fab-gotcha-timing" tone="warning">

Forward duration is **300ms** (raised from upstream's 200ms so the stagger cascade is legible). Reverse stays 200ms. If you change `_staggerSpread`, re-test with 4+ items — the cascade collapses if the spread window is too narrow.

</Callout>

### Re-sync procedure (shared across all four forks)

When bumping upstream (`fab_m3e`, `navigation_rail_m3e`, `split_button_m3e`, or `toolbar_m3e`):

1. **Bump** the fork's `pubspec.yaml` `version:` and the root `pubspec.yaml` override comment's stated version.
2. **Backfill** the fork's `CHANGELOG.md` with the new version entry (every fork's CHANGELOG is stale — see §11).
3. **Diff** each patched file against the new upstream release of the same path.
4. **Re-apply** each `// Patched (vendored fork)` / `// Fork patch:` / `// PATCH` block by hand. Do NOT copy upstream over the fork blindly — the markers are your only map of what to preserve.
5. **Update** the root `pubspec.yaml` override comment if the WHY or the patched-file list changed.
6. **Run** the fork's `flutter test` in `packages/<name>/`, then run the consuming showcase (`stacked_kit_showcase`) to confirm the patched behaviour still renders.

### Override hygiene (shared)

<Callout id="fab-override-hygiene" tone="warning">

Never remove a `dependency_overrides` `path:` entry without first confirming upstream includes the fix. A "clean" `flutter pub get` silently reverts the fork to the pub.dev release, and the patch — including its bug fix — disappears with no compile error. The root override comment is the only durable record of why the patch exists.

</Callout>

<Callout id="fab-doc-gap" tone="risk">

Doc-gap debt every fork carries: (a) no `> Vendored fork of upstream <name> <version>` banner in the README; (b) stale `CHANGELOG.md`; (c) `EmilyMonestone` → `EmilyMoonstone` typo in `issue_tracker` (present in every fork's `pubspec.yaml`).

</Callout>

### Decisions / ADRs

None. The fork contract is documented in the root `pubspec.yaml` override comment; no separate ADR was written. If the patch grows beyond one file, write one.

### Changelog & status

The fork's `CHANGELOG.md` is **stale** — it only contains `## 0.1.0 - Changelog initialized.` There is no `0.1.1` entry documenting the fork patch.

**Backfill task:** add a `## 0.1.1` entry describing the staggered-spring reveal, the 200→300ms forward duration, the no-scrim behaviour, and the `Interval.transform` leak fix.

### Open questions

<QuestionForm id="fab-open-questions" questions={[
  {
    id: "fab-q-resync-cadence",
    title: "How often should we re-sync against upstream fab_m3e?",
    subtitle: "Upstream is on pub.dev; the fork drifts further every release.",
    mode: "single",
    options: [
      { id: "each-release", label: "Every upstream release", detail: "Lowest drift; highest maintenance." },
      { id: "quarterly", label: "Quarterly", detail: "Batch re-syncs; risk of stale Flutter SDK constraints.", recommended: true },
      { id: "only-on-bug", label: "Only when upstream fixes a bug we hit", detail: "Lowest effort; highest drift risk." },
    ],
    required: false,
  },
  {
    id: "fab-q-upstream-pr",
    title: "Should the staggered-spring patch be upstreamed as a PR?",
    mode: "freeform",
    placeholder: "Note any blockers (e.g. upstream API freeze, different motion spec interpretation).",
  },
]} submitLabel="Record answers" />
