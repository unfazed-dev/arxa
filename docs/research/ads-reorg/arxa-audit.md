# arxa repo audit — pre-move to /Volumes/business_ssd/arxa_digital_solutions/arxa

Fact report only. No files in the repo were moved, edited, or committed for this audit; the only write is this file.

Generated: 2026-09-02. Repo: `/Volumes/developer_ssd/Developer/totem_labs/arxa`, branch `main` @ `bdcb530f`.

## 1. Structure

Total working tree: **53G**. Top-level dirs by size:

| Dir | Size | Purpose |
|---|---|---|
| `kit/` | 22G | Flutter package monorepo — `arxa_kit_*` packages (core, ui_library, cairn, showcase_app, studio_transport, etc.) consumed by `mobile_flutter/` via path deps |
| `desktop/` | 18G | Tauri v2 desktop shell (`solutions.arxadigital.arxa`) — native window over the served arxa studio web UI + bundled engine/CLI sidecars |
| `mobile_flutter/` | 7.8G | `arxa_studio_mobile` — the Flutter mobile app (FlutterFlow/Stacked-style views: code_shell, studio_shell, approvals_shell) |
| `mobile/` | 2.4G | Separate Tauri v2 **mobile** scaffold (pairing/iroh transport, `mobile/src-tauri`) — distinct from `mobile_flutter/` |
| `archives/` | 1.7G | Retired material, explicitly "nothing here is live." Old `app-box`/`appbox` prototypes, pre-Dart tooling, design v1/v2 |
| `docs/` | 116M | Plans, research, INDEX, VOCABULARY — mostly plan history (110 files in `docs/plans/`) |
| `designs/` | 35M | Design artifacts |
| `arxa/` | 34M | The arxa daemon — pure Dart, `dart:io` only, no third-party runtime deps. Pipeline phases/gates FSM, web builder UI server |
| `node_modules/` | 33M | JS deps for root `package.json` |
| `skills/` | 30M | Source-of-truth for 13 `arxa-*` Claude skills, symlinked in from `~/.claude/skills` and `~/.kimi-code/skills` (see §2) |
| `logs/`, `config/`, `gates/`, `deploy/`, `pipeline/`, `tools/`, `harness/`, `memory/`, `supabase/`, `hooks/`, `build/`, `scripts/` | ≤1.4M each | Small, config/orchestration/policy dirs |

**Build/cache/junk (all git-untracked, confirmed via `git ls-files`, none of these need to move — they regenerate):**

| Path | Size |
|---|---|
| `desktop/src-tauri/target/` | 18G |
| `mobile_flutter/build/` | 4.9G |
| `kit/showcase_app/build/` | 4.9G |
| `kit/studio_transport/rust/target/` | 4.7G |
| `kit/tools/device_input_probe/build/` | 2.7G |
| `mobile/src-tauri/gen/apple/build/` + `Externals/` | ~2.4G |
| `node_modules/`, `.build/`, `.claude-flow/`, `.kit/` | ~60M |

**≈37.7G of the 53G tree (≈71%) is regenerable build output**, not source. A move can skip these entirely and let each toolchain rebuild at the destination — cuts the transfer to ≈15G.

`archives/spikes/` (1.7G) is git-tracked history, not junk — it's dead weight (retired, "do not cite as current") but is committed content, so it moves with the repo unless someone deliberately prunes it.

## 2. Move breakage

**(a) Absolute `/Volumes/developer_ssd` paths — 3 kinds:**

- **docs/plans/ (6 files, mostly copy-paste `cd` commands in runbooks):** `arxa-harness-and-distribution.md:42`, `arxa-lens-full-port.md` (19 occurrences, lines 338–3315), `arxa-rename-handoff-response.md:3`, `glass-chrome-root-cause-fixes.md:155`, `handoff-reveal-drawer-remaining.md:13`, `media-3d-animation-games.md` (~20 occurrences). All are inert prose/instructions in historical plans — they don't execute, so they go stale silently rather than break anything live.
- **docs/ (non-plans, 2 files):** `docs/INDEX.md:214,217,218` (paths to *other* repos: `stacked_kit`, `factory/flutter-crew`, `applications/p2` — all confirmed to still exist on disk, outside the repos being moved, so unaffected) and `docs/probe-mutation-equivalence-waveD.md:32`.
- **Code:** zero absolute-path hits in `.dart/.js/.ts/.sh/.py` source outside build dirs. The only files matched were generated `flutter_export_environment.sh` (regenerated by `flutter build`, harmless) and dart tool scripts under `arxa/tool/*probe*.dart` / `skills/arxa-designer/gates/*.js` that merely have "probe" in the filename, not an absolute-path dependency — grep matched on filename, not content; worth a closer look but not urgent.
- **Config/JSON/YAML:** everything matched is generated (`.claude-flow/sessions/*.json`, Swift build `.build/index-build/*` maps) — none are hand-maintained config.

**No absolute-path leaks found in `.env*`, deploy configs, or `Info.plist`.**

**(b) Symlinks inside repo — 821 total** (excluding `node_modules`, `.git`): 499 in `kit/`, 294 in `mobile_flutter/`, 23 in `archives/spikes/`, plus one each in `skills/`, `mobile/`, `.kimi-code/`, `.claude/`, `.agents/`. All are either:
- relative and repo-internal (`.claude/skills -> ../skills`, `.kimi-code/skills -> ../skills`, `.agents/skills -> ../skills`, framework `Versions/Current` links) — **survive a move untouched**, or
- absolute but pointing *outside* the repo to `~/.pub-cache/...` or `/Volumes/developer_ssd/dev/fvm/...` (CocoaPods/Flutter plugin symlinks under `ios/Flutter/ephemeral`, `ios/.symlinks`) — these targets are **not** moving, so they still resolve after the arxa repo moves. Worst case they're stale build cache and `flutter clean`/`pod install` regenerates them anyway.

**No repo-internal symlink breaks from the move itself.**

**(c) Inbound symlinks — this is the real breakage.** 13 skill directories under `skills/` are the SSOT for arxa's Claude skills, and they're symlinked in from two places, each with all 13 links:
- `~/.claude/skills/{arxa-builder,arxa-cicd,arxa-deployer,arxa-designer,arxa-intake,arxa-lens,arxa-lint,arxa-moodboarder,arxa-orchestrator,arxa-reviewer,arxa-scaffolder,arxa-story-mapper,arxa-tester}` → `/Volumes/developer_ssd/Developer/totem_labs/arxa/skills/<name>`
- `~/.kimi-code/skills/<same 13 names>` → same targets (this is also what `~/.agents/skills` resolves to, since `~/.agents/skills -> ~/.kimi-code/skills`)

**26 symlinks total (13 unique targets × 2 root locations) will break the moment the repo path changes** — every one hardcodes `/Volumes/developer_ssd/...`. These need to be recreated pointing at the new `/Volumes/business_ssd/arxa_digital_solutions/arxa/skills/<name>` path as part of the move. Nothing in `~/.agents/skills.shared` points into this repo.

**(d) Path dependencies — all relative, all safe to move as a unit:**
- Flutter: 8 `pubspec.yaml` files use `path:` deps, all relative (`../kit/core`, `../core`, `vendor/fab_m3e`, etc.) — `mobile_flutter`, `kit/{core,cairn,data,ui_library,showcase_app}`, `arxa`. None point outside the repo.
- `kit/cairn/pubspec.yaml` also has a **git** dependency: `github.com/unfazed-dev/cairn.git` pinned to ref `fabc1a163953c27aba33617dae72e704bcef4e11`, path `sdk/cairn_flutter` — fetched via pub's git cache, not a local path dep to the sibling `cairn` repo at `/Volumes/developer_ssd/Developer/cairn`. **arxa does not locally path-depend on the cairn repo.**
- Rust: one `Cargo.toml` path dep — `desktop/src-tauri/Cargo.toml:34`: `arxa-mobile = { path = "../../mobile/src-tauri" }` — relative, reaches `mobile/` (same repo), safe.
- No `.gitmodules`, no `package.json` `file:` deps, single `git worktree` (this checkout itself).

**(e) Env/config:** only one `.env*` file in the repo, `deploy/remote/.env.example` — a template, no live secrets or paths to check.

## 3. Stale references (bounded to most consequential; not all 30 slots needed — fewer real problems than expected)

1. **`docs/plans/widget-panel-vocabulary-reconciliation.md:111`** and **`docs/plans/inspector-everything-as-widgets.md:21`** instruct running `~/.agents/skills/consultant/scripts/consult.sh gate skill ...` — per the operator's own global CLAUDE.md, that script **no longer exists**; it was replaced by `consult-mode`. Two other plans (`no-single-letter-identifiers-law.md:18`, `screen-vocabulary-identifier-rename.md:43`, `flows-answers-ssot-and-canvas-undo.md:851`) already note the script is missing — those 3 are self-aware and fine; the 2 above still tell a reader to run a dead command.
2. **Gate count, minor discrepancy:** commit `bdcb530f` claims "thirteen in lib, fourteen in gateOrder." `gate_runner.dart`'s `gateOrder` list does have exactly 14 entries (confirmed). But `arxa/lib/` has 15 `gate_*.dart` files besides `gate_runner.dart` itself, and 2 of them — `gate_design_styles.dart`, `gate_design_widgets.dart` — are **not referenced in `gateOrder`** at all. Low-confidence finding (naming may not map 1:1 file-to-gate); flagged for someone closer to the code to confirm.
3. Everything else checked came back **consistent, not stale**: `pipeline/README.md`'s claim that `pipeline/pipeline.sh` and `gates/run_all.sh` "never landed" — confirmed both are absent; `harness/README.md`'s claim of one policy file `hooks/arxa-guard.js` — confirmed present; `phases.dart`, `pipeline_fsm.dart`, `pipeline/state/default.state.json` all exist as documented.
4. **"probe-runner" mentions (13 files, e.g. `arxa/README.md:15-16`, `docs/VOCABULARY.md:414-418`, `docs/INDEX.md`):** all correctly describe it as an *archived* tool ("never reach back for the archived probe-runner") — not stale, intentional historical pointer. Note: a skill literally named `probe-runner` does exist in this Claude session's skill list (different, unrelated skill-plugin namespace) — no conflict, just worth knowing the name is reused elsewhere.
5. **"app-box"/"appbox" mentions outside `archives/` (2 files):** `arxa-rename-handoff-response.md`, `h7-full-rename-pass.md` — both are the rename-tracking plans themselves, discussing the old name deliberately. Not stale.
6. **`arxa-studio` referenced 93 times across docs, but no `arxa-studio/` directory exists in this repo** (confirmed via `ls`) — every reference assumes the sibling repo at `/Volumes/developer_ssd/Developer/totem_labs/arxa-studio`. This is expected (arxa-studio is genuinely a separate repo per the task brief) but means **93 doc references become cross-repo references that must both move in lockstep** for any relative reasoning in these docs to still make sense — they're prose pointers, not paths, so nothing breaks mechanically, but a reader following them needs both repos moved together.

## 4. Plans inventory

**Total: 110 files in `docs/plans/` (flat, non-recursive count).**

12 most recently modified:

| Plan | Status (from header) |
|---|---|
| `b2-sync-first-phase1.md` | Active — implementation plan, checkbox-tracked |
| `entitlement-backend-runbook.md` | Active — "hosted-half source code-complete, not deployed" (2026-08-05) |
| `arxa-kit-cairn.md` | Active — revised 2026-08-27 after a grilling/verification session |
| `doorbell-decision-2026-08-29.md` | Mostly done — "B1 mechanism SHIPPED same-day" |
| `mobile-pairing-transport.md` | Active — wiring stubbed seams |
| `arxa-rename-handoff-response.md` | Done — "every finding closed," 14 commits |
| `arxa-dial-undo-redo.md` | Decisions locked 2026-08-26, implementation pending |
| `inline-generative-ui-in-dsh.md` | Active — "research complete, decisions proposed, not yet ratified" |
| `h7-full-rename-pass.md` | Open (2026-08-26) |
| `dsh-isolation-from-operator-install.md` | Done — postmortem/investigation writeup |
| `arxa-harness-and-distribution.md` | Decisions locked 2026-08-21 |
| `widget-panel-vocabulary-reconciliation.md` | Decisions locked, execution pending go-ahead |

**Done/superseded-looking (explicit COMPLETE/DONE/SHIPPED/SUPERSEDED/CLOSED in header): 15 of 110** — candidate archive list (names only):
`arxa-dart-only-tooling.md`, `arxa-data-model-decisions.md`, `arxa-lens-full-port.md`, `arxa-memory-and-payment.md`, `arxa-rename-handoff-response.md`, `doorbell-decision-2026-08-29.md`, `entitlement-backend-runbook.md`, `glass-chrome-root-cause-fixes.md`, `increment-3-edit-arming-resize-handles.md`, `inline-generative-ui-in-dsh.md`, `lens-daemon.md`, `media-3d-animation-games.md`, `rung-to-viewport-sweep.md`, `screen-reveal-drawer-composer-tools-logic.md`, `studio-v2-emit-blockers.md`.

Remaining ~95 plans were not individually header-checked (out of scope for the word budget) — likely a mix of active and quietly-stale; a full sweep would need per-file status classification beyond this audit.

**Plans referencing cairn or arxa-studio:** at least `arxa-kit-cairn.md`, `b2-sync-first-phase1.md` (cairn-server sidecar), `doorbell-decision-2026-08-29.md` (arxa-studio plugin work), `inline-generative-ui-in-dsh.md` ("lands in arxa-studio"), `h7-full-rename-pass.md` (arxa-studio commit reference), plus the 93 general arxa-studio mentions counted in §3.

## 5. Uncommitted work

`git status --short`: **40 files** (32 modified, 8 untracked) — matches the ~40 figure in the task brief. All of it is one coherent in-progress changeset, not junk:

- **mobile_flutter UI refactor (bulk of it):** `code_shell/code_conversation/*` and `code_shell/code_sessions/*` — each view is being split into `.mobile.dart`/`.tablet.dart` variants plus new `_body.dart` files (untracked), alongside a new `ui/widgets/` dir, `test/widgets/`, `ui/app_transitions.dart`, and `services/shot_extension.dart` (all untracked) — looks like a responsive-layout extraction in progress.
- **App wiring:** `app.dart`, `app.router.dart`, `app_data.dart`, `main.dart`, `code_shell_chrome.dart`, plus new `lib/data/conversation/` (untracked) — supporting the same refactor.
- **Localization:** `app_en.arb`, `app_localizations.dart`, `app_localizations_en.dart` — new strings for the above.
- **Studio/approvals shells:** `studio_session_view.dart`/`_viewmodel.dart`, `approvals_list_view.dart` — smaller touches, same effort.
- **Build config:** `pubspec.yaml`/`.lock`, `ios/Runner/Info.plist`, new `mobile_flutter/.config/` (untracked).
- **Unrelated small changes:** `arxa/lib/lens/motion.dart`, `arxa/lib/lens_cli.dart`, `docs/research/pipeline-map.md`, `kit/cairn/pubspec.lock` (dependency bump), and a `skills/arxa-cicd/` doc/reference update (`CICD_playbook.mdx`, `SKILL.md`, `ci-explainer.html`, `pr-process.md`).

Nothing here looks abandoned or move-blocking — it's active feature work that should just be committed (or stashed) before any repo relocation, same as any other dirty working tree.

## 6. Cross-repo linkage

- **cairn:** consumed as a Flutter package (`kit/cairn/`) with a **git dependency** on `github.com/unfazed-dev/cairn.git` (pinned ref, fetched into pub's global git cache) — not a local filesystem path dependency on `/Volumes/developer_ssd/Developer/cairn`. 10 doc mentions of "cairn" outside `kit/cairn/` and `archives/`. `desktop/` also runs a `cairn-server`/`cairn-pushd` Rust sidecar per `docs/plans/b2-sync-first-phase1.md`.
- **arxa-studio:** referenced only in prose/docs (93 mentions), never as a path dependency or symlink — it's the served web UI that `desktop/` and `mobile_flutter/` point at over HTTP/local serving, not a code dependency of this repo.
- **"Arxa Digital Solutions" / business identity:** 4 legitimate product mentions (`desktop/README.md:8,252`, `docs/plans/mobile-pairing-transport.md:28`, `docs/ci/self-hosted-runners.md:36`) — all are the actual product/company name (`solutions.arxadigital.arxa` bundle ID, ownership-boundary rules), not reorg-planning leakage. **No mentions of `business_ssd` or `arxa_digital_solutions` as a filesystem path anywhere in the repo** — the destination path isn't referenced or assumed by any doc or config.

## Summary of what would need to change on move

1. Recreate 26 skill symlinks (`~/.claude/skills/arxa-*`, `~/.kimi-code/skills/arxa-*`) at the new path — the only mechanical breakage found.
2. Optionally strip ≈37.7G of untracked build/cache dirs before transfer (list in §1) — nothing is lost, everything regenerates.
3. Leave the ~28 stale absolute-path mentions in `docs/plans/*.md` alone or fix opportunistically — they're inert prose, not live breakage.
4. Commit or stash the 40 dirty mobile_flutter files before moving, same as any working tree.
5. No `.gitmodules`, no broken internal path deps, no local filesystem dependency on the cairn or arxa-studio repos to worry about.
