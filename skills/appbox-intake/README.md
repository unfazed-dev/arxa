The intake skill (plan 10). An optional phase that elicits a brief and seeds the registry.json the designer consumes. Belongs: the elicitation engine (`appboxd/lib/intake.dart` — `appbox intake emit|seed|validate`), its answers schema, and artefact emission. Does not belong: design or code generation, the desktop wizard UI (app/), or the traceability gate (gates/).
