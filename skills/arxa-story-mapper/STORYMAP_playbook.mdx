<Callout id="header" tone="info">
# arxa-story-mapper — Per-Skill Playbook

`arxa-story-mapper` · status: **active**

**Role (one line):** elicit requirements as an Epic → Feature → Story map (MoSCoW priorities, release swimlanes) and emit the unified `docs/intake/brief.md` + `story-map.json` + interactive HTML — the tail of the intake chain, handing off to `arxa-designer`.

**Scope of this document:** lets a human OR an LLM run the story-map phase without reading the skill source. Folded from `SKILL.md`.

**Pipeline position:** stage 0 (optional — pre-intake standalone, or answer-driven after `arxa-intake`) — downstream `arxa-moodboarder` (slices `story-map.json`), then `arxa-designer` (consumes `docs/intake/brief.md`). Full chain, stage numbers, per-stage artifacts: `docs/research/pipeline-map.md` §1.

## Table of contents

0. Header & TOC · 1. Overview & role · 2. Inputs · 3. Procedure · 4. Outputs · 5. Rules · 6. References

</Callout>

### Overview & role

The story-mapper elicits; it does not generate (architecture §22) — no views, viewmodels, routes, or layouts; the map is the client's words, structured. With intake answers the emitted brief is **unified** (intake sections first, then Releases, hierarchy, surface inventory); standalone (intake is optional, plan 10.7) features derive surfaces as before, flagged ` — [inferred]`. The mapping is enforced by the emitter, not prose: Epic→shell (slug = the name's first ascii word, lowercase), Feature→surface (`id = <epic-slug>.<feature-slug>` matching `^([a-z][a-z0-9]*)\.([a-z][a-z0-9]*)$`), Story→requirement listed under its feature, MoSCoW + release rolled up per surface (strongest live priority, earliest live release), all-`wont` features → Out of scope.

### Inputs

Product requirements from the user — project name, 2–8 epics, 1–6 features per epic, 1–10 stories per feature, priority (must/should/could/wont) and release per story; optional points, description, locales + default locale. Optional intake answers: `--answers`, auto-discovered from `pipeline/state/run.intake.json` then `pipeline/state/default.intake.json`.

### Procedure

Gather per the conversation guide (opening → per-epic walkthrough → confirm priorities → confirm releases), building the JSON per the SKILL.md field reference (`releases[].name` must match each story's `release`; `priority` is validated). Emit one command — `arxa emit story-map --input data.json --output docs/intake/story_map.html --data-out docs/intake/story-map.json --brief-out docs/intake/brief.md` (stdin without `--input`; add `--answers` when chaining). Self-check with `arxa emit story-map --self-test` (slugs, gate parse, all-wont rule), then `arxa gate intake` for registry↔brief traceability both ways. Hand off: `brief.md` + `story-map.json` to the designer, `story_map.html` to the client for scope confirmation; optionally `arxa intake seed --brief docs/intake/brief.md`.

### Outputs

`docs/intake/story_map.html` — self-contained interactive map (three-tier cards, MoSCoW colors, release swimlanes, stats panel); `docs/intake/story-map.json` — the full-fidelity machine handoff (priorities, releases, points); `docs/intake/brief.md` — the unified brief with provenance-marked intake fields (`client`|`founder`|`inferred`, `inferred` visible), `## Surface inventory`, and `## Layout template` when elicited.

### Rules

With answers, the surface inventory IS the intake-declared surfaces: features pin by explicit `id` equal to a declared surface id, and only unmatched derivations are flagged ` — [inferred]`. This skill never binds a `surface` to registry entries — intake names; the designer binds. CJK-only names slug to `s` — prefer readable slugs and confirm derived ids with the user before emitting. Over 50 stories → consider splitting into sub-maps. Keep the standard layout (all three artifacts under `docs/intake/`) so `arxa gate intake` needs no flags.

<!-- kb:begin -->
### References
- `story-map.schema.json` (skill-local) — the data contract. `LICENSE.txt` — MIT, third-party skill adapted for arxa.
- `arxa emit story-map` (`arxa/lib/`) — the emitter + `--self-test` handoff check. `arxa gate intake` (`arxa/lib/gate_intake.dart`) — the traceability gate. `arxa intake seed` — additive registry seeding from the brief.
<!-- kb:end -->
