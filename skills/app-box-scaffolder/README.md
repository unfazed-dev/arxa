The scaffolder skill (plan 03 / dogfood 14.8). Scaffolds app surfaces from a frozen structure.json + target set, emitting the per-surface Dart files that match the target-DERIVED form-factor set. Belongs: the scaffold engine (scaffold.py) and its manifest emission. Does not belong: widget bodies (app-box-builder), platform ceremony files (app-box-deployer), design judgement (app-box-reviewer), or the coverage gate that consumes this skill's output (gates/coverage).
