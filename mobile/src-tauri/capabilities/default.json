{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the arxa studio mobile shell window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
