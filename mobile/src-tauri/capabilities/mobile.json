{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-scanner",
  "description": "QR pairing (decision M2): camera barcode scanning, mobile targets only.",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["barcode-scanner:default"]
}
