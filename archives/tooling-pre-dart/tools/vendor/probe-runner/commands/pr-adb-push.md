---
description: "DEPRECATED — use /pr-adb-intent. Broadcasts an intent via am broadcast (NOT adb push file copy; for files use /pr-adb-files)."
allowed-tools: Bash(python3 *)
argument-hint: "args for scripts/adb_intent.py (forwarded; prints deprecation to stderr)"
---

!`python3 ${CLAUDE_SKILL_DIR}/scripts/adb_push.py $ARGUMENTS`
