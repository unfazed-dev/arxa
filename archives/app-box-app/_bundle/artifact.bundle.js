// AUTO-GENERATED by tools/bundle_viewmodels.js (plan 09 step 9.3). Do not edit.
// The frozen design's viewmodels bundled into one IIFE the embedded JS engine
// evaluates once. Viewmodel bodies are preserved verbatim; only import/export
// is lowered and Node's fs is bridged to the host (readFixture unchanged).
(function (globalThis) {
var URL = (function () {
  function normJoin(baseDir, rel) {
    var parts = (baseDir + '/' + rel).split('/');
    var out = [];
    for (var i = 0; i < parts.length; i++) {
      if (parts[i] === '' || parts[i] === '.') continue;
      if (parts[i] === '..') { if (out.length) out.pop(); continue; }
      out.push(parts[i]);
    }
    return out.join('/');
  }
  function URL(rel, base) {
    if (base && base.indexOf('file://') === 0) {
      var b = base.slice(7);
      var dir = b.slice(0, b.lastIndexOf('/'));
      this.href = 'file://' + normJoin(dir, rel);
    } else {
      this.href = rel;
    }
  }
  URL.prototype.toString = function () { return this.href; };
  return URL;
})();

  var __modules = {};
  var __cache = {};
  var __resolveMap = {
    "app.routes.js\n./ui/views/stage_shell/stage_shell_viewmodel.js": "ui/views/stage_shell/stage_shell_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/projects/home/home_viewmodel.js": "ui/views/stage_shell/projects/home/home_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/projects/new/new_viewmodel.js": "ui/views/stage_shell/projects/new/new_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/design/directions/directions_viewmodel.js": "ui/views/stage_shell/design/directions/directions_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/design/surface/surface_viewmodel.js": "ui/views/stage_shell/design/surface/surface_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/design/approve/approve_viewmodel.js": "ui/views/stage_shell/design/approve/approve_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/build/run/run_viewmodel.js": "ui/views/stage_shell/build/run/run_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/build/finding/finding_viewmodel.js": "ui/views/stage_shell/build/finding/finding_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/build/approve/approve_viewmodel.js": "ui/views/stage_shell/build/approve/approve_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/ship/targets/targets_viewmodel.js": "ui/views/stage_shell/ship/targets/targets_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/ship/confirm/confirm_viewmodel.js": "ui/views/stage_shell/ship/confirm/confirm_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/chat/home/home_viewmodel.js": "ui/views/stage_shell/chat/home/home_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/settings/credentials/credentials_viewmodel.js": "ui/views/stage_shell/settings/credentials/credentials_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/settings/devices/devices_viewmodel.js": "ui/views/stage_shell/settings/devices/devices_viewmodel.js",
    "app.routes.js\n./ui/views/stage_shell/settings/kits/kits_viewmodel.js": "ui/views/stage_shell/settings/kits/kits_viewmodel.js",
    "ui/views/stage_shell/stage_shell_viewmodel.js\n../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/projects/home/home_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/projects/home/home_viewmodel.js\n../../../../../services/facades/project_facade.js": "services/facades/project_facade.js",
    "ui/views/stage_shell/projects/new/new_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/design/directions/directions_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/design/surface/surface_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/design/approve/approve_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/build/run/run_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/build/run/run_viewmodel.js\n../../../../../services/facades/pipeline_facade.js": "services/facades/pipeline_facade.js",
    "ui/views/stage_shell/build/finding/finding_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/build/finding/finding_viewmodel.js\n../../../../../services/facades/pipeline_facade.js": "services/facades/pipeline_facade.js",
    "ui/views/stage_shell/build/approve/approve_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/ship/targets/targets_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/ship/targets/targets_viewmodel.js\n../../../../../services/facades/catalog_facade.js": "services/facades/catalog_facade.js",
    "ui/views/stage_shell/ship/confirm/confirm_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/ship/confirm/confirm_viewmodel.js\n../../../../../services/facades/pipeline_facade.js": "services/facades/pipeline_facade.js",
    "ui/views/stage_shell/chat/home/home_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/chat/home/home_viewmodel.js\n../../../../../services/facades/chat_facade.js": "services/facades/chat_facade.js",
    "ui/views/stage_shell/settings/credentials/credentials_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/settings/devices/devices_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/settings/devices/devices_viewmodel.js\n../../../../../services/facades/catalog_facade.js": "services/facades/catalog_facade.js",
    "ui/views/stage_shell/settings/kits/kits_viewmodel.js\n../../../../../services/facades/shell_facade.js": "services/facades/shell_facade.js",
    "ui/views/stage_shell/settings/kits/kits_viewmodel.js\n../../../../../services/facades/catalog_facade.js": "services/facades/catalog_facade.js",
    "services/facades/shell_facade.js\n../repositories/pipeline_repository.js": "services/repositories/pipeline_repository.js",
    "services/facades/project_facade.js\n../repositories/project_repository.js": "services/repositories/project_repository.js",
    "services/facades/pipeline_facade.js\n../repositories/pipeline_repository.js": "services/repositories/pipeline_repository.js",
    "services/facades/catalog_facade.js\n../repositories/kit_repository.js": "services/repositories/kit_repository.js",
    "services/facades/catalog_facade.js\n../repositories/device_repository.js": "services/repositories/device_repository.js",
    "services/facades/catalog_facade.js\n../repositories/target_repository.js": "services/repositories/target_repository.js",
    "services/facades/chat_facade.js\n../repositories/chat_repository.js": "services/repositories/chat_repository.js",
    "services/repositories/pipeline_repository.js\n./fixture_reader.js": "services/repositories/fixture_reader.js",
    "services/repositories/project_repository.js\n./fixture_reader.js": "services/repositories/fixture_reader.js",
    "services/repositories/kit_repository.js\n./fixture_reader.js": "services/repositories/fixture_reader.js",
    "services/repositories/device_repository.js\n./fixture_reader.js": "services/repositories/fixture_reader.js",
    "services/repositories/target_repository.js\n./fixture_reader.js": "services/repositories/fixture_reader.js",
    "services/repositories/chat_repository.js\n./fixture_reader.js": "services/repositories/fixture_reader.js",
    "services/repositories/fixture_reader.js\nnode:fs": "__virtual__:node:fs"
  };
  function __resolve(importerKey, spec) {
    var k = __resolveMap[importerKey + '\n' + spec];
    if (!k) throw new Error('module not resolved: ' + spec + ' (from ' + importerKey + ')');
    return k;
  }
  function __load(key) {
    if (__cache[key]) return __cache[key].exports;
    var factory = __modules[key];
    if (!factory) throw new Error('module factory missing: ' + key);
    var mod = { exports: {} };
    __cache[key] = mod;
    factory.call(mod.exports, mod.exports, function (spec) { return __load(__resolve(key, spec)); });
    return mod.exports;
  }
__modules["app.routes.js"] = function (__exports, __req) {
// Routes are DERIVED from models/screens_model/registry.json — one GET per
// buildable entry, plus the POSTs that mutate. The arrow never reverses.
const shell = __req("./ui/views/stage_shell/stage_shell_viewmodel.js");
const projects_home = __req("./ui/views/stage_shell/projects/home/home_viewmodel.js");
const projects_new = __req("./ui/views/stage_shell/projects/new/new_viewmodel.js");
const design_directions = __req("./ui/views/stage_shell/design/directions/directions_viewmodel.js");
const design_surface = __req("./ui/views/stage_shell/design/surface/surface_viewmodel.js");
const design_approve = __req("./ui/views/stage_shell/design/approve/approve_viewmodel.js");
const build_run = __req("./ui/views/stage_shell/build/run/run_viewmodel.js");
const build_finding = __req("./ui/views/stage_shell/build/finding/finding_viewmodel.js");
const build_approve = __req("./ui/views/stage_shell/build/approve/approve_viewmodel.js");
const ship_targets = __req("./ui/views/stage_shell/ship/targets/targets_viewmodel.js");
const ship_confirm = __req("./ui/views/stage_shell/ship/confirm/confirm_viewmodel.js");
const chat_home = __req("./ui/views/stage_shell/chat/home/home_viewmodel.js");
const settings_credentials = __req("./ui/views/stage_shell/settings/credentials/credentials_viewmodel.js");
const settings_devices = __req("./ui/views/stage_shell/settings/devices/devices_viewmodel.js");
const settings_kits = __req("./ui/views/stage_shell/settings/kits/kits_viewmodel.js");

const __default = [
  ['GET',  '/', projects_home.page],
  ['GET',  '/projects/new', projects_new.page],
  ['GET',  '/design', design_directions.page],
  ['GET',  '/design/surface', design_surface.page],
  ['GET',  '/design/approve', design_approve.page],
  ['GET',  '/build', build_run.page],
  ['GET',  '/build/finding', build_finding.page],
  ['GET',  '/build/approve', build_approve.page],
  ['GET',  '/ship', ship_targets.page],
  ['GET',  '/ship/confirm', ship_confirm.page],
  ['GET',  '/chat', chat_home.page],
  ['GET',  '/settings', settings_credentials.page],
  ['GET',  '/settings/devices', settings_devices.page],
  ['GET',  '/settings/kits', settings_kits.page],
  ['POST', '/design/approve/approve', design_approve.approve],
  ['POST', '/build/approve/approve', build_approve.approve],
  ['POST', '/ship/confirm/release', ship_confirm.approve],
  ['POST', '/design/surface', design_surface.submit],
  ['POST', '/build', build_run.submit],
  ['POST', '/chat', chat_home.submit],
  ['POST', '/projects/new', projects_new.submit],
  ['POST', '/prefs/theme', shell.theme],
];

// Required and non-empty: the landing route of every tab. The scaffolder
// cannot derive this — a tab whose root is unknown gets an invented one.
const tabRoots = {
  projects: '/',
  design:   '/design',
  build:    '/build',
  ship:     '/ship',
  chat:     '/chat',
  settings: '/settings',
};

__exports.default = __default;
__exports.tabRoots = tabRoots;
};
__modules["ui/views/stage_shell/stage_shell_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'stage.shell';

// The shell renders the chrome; surfaces fill its {% block surface %}.
const { chrome } = __req("../../../services/facades/shell_facade.js");

const page = (c, h) =>
  h.render(c, 'ui/views/stage_shell/stage_shell_view.html', chrome('projects'));

// Appearance is a preference, so it round-trips to the server and comes back
// as a full refresh. No client-side theme switching exists.
// 'system' is stored explicitly rather than as an absent value, so choosing it
// after an override is a real choice and not a reset.
const THEMES = ['system', 'light', 'dark'];

const theme = async (c, h) => {
  const body = await c.req.parseBody();
  const t = THEMES.includes(body.theme) ? body.theme : 'system';
  h.setPrefs(c, { theme: t });
  // refresh-exempt: the theme is a `data-theme` attribute on #app, which sits
  // outside every swap target — no fragment can reach it. This is the one
  // reload in the artifact, and helpers.mjs names it as the escape hatch.
  return h.refresh(c);
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.theme = theme;
};
__modules["ui/views/stage_shell/projects/home/home_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'projects.home';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { list } = __req("../../../../../services/facades/project_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["list", "empty", "loading"];

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'list';
  return h.render(c, 'ui/views/stage_shell/projects/home/home_view.html', {
    ...chrome('projects'), state, states: STATES, base: '/',
    ...list(),
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
};
__modules["ui/views/stage_shell/projects/new/new_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'projects.new';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["form", "validating", "error"];
const VIEW = 'ui/views/stage_shell/projects/new/new_view.html';

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'form';
  return h.render(c, VIEW, {
    ...chrome('projects'), state, states: STATES, base: '/projects/new',
    name: state === 'form' ? '' : 'Ledgerly',
  });
};

// Validation is the server's, and the answer is 422 + the form again. The
// typed name comes back with it: an error that empties the field you got right
// is a second error.
//
// A valid submit hands the browser to the list. The prototype does not persist
// the project, so nothing on that list claims to be the one you just typed —
// inventing a row is the sort of lie the honesty surfaces exist to rule out.
const submit = async (c, h) => {
  const body = await h.form(c);
  const name = String(body.name ?? '').trim();
  if (!String(body.client ?? '').trim()) {
    return h.render(c, `${VIEW}#form`,
      { ...chrome('projects'), state: 'error', name }, 422);
  }
  return h.location(c, '/');
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.submit = submit;
};
__modules["ui/views/stage_shell/design/directions/directions_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'design.directions';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");


// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["three-up", "approved"];

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'three-up';
  return h.render(c, 'ui/views/stage_shell/design/directions/directions_view.html', {
    ...chrome('design'), state, states: STATES, base: '/design',
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
};
__modules["ui/views/stage_shell/design/surface/surface_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'design.surface';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["live", "stale"];
const VIEW = 'ui/views/stage_shell/design/surface/surface_view.html';

const stateOf = (c, h) => {
  const q = c.req.query('state');
  if (STATES.includes(q)) return q;
  return h.session(c).data.surfaceStale ? 'stale' : 'live';
};

const page = (c, h) =>
  h.render(c, VIEW, {
    ...chrome('design'), state: stateOf(c, h), states: STATES, base: '/design/surface',
  });

// Re-render clears the staleness the panel was complaining about, then swaps
// the panel that was complaining. Write first, render from the write.
const submit = (c, h) => {
  h.session(c).data.surfaceStale = false;
  return h.render(c, `${VIEW}#panel`, { ...chrome('design'), state: 'live' });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.submit = submit;
};
__modules["ui/views/stage_shell/design/approve/approve_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'design.approve';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["pending", "approved"];
const VIEW = 'ui/views/stage_shell/design/approve/approve_view.html';

// An explicit ?state= is the designer's override; otherwise the gate reports
// what the session actually records. Swapping an "approved" fragment in while
// a reload would still render "pending" is a worse bug than the full reload it
// replaces — so the write happens first and the swap renders from it.
const stateOf = (c, h) => {
  const q = c.req.query('state');
  if (STATES.includes(q)) return q;
  return h.session(c).data.designApproved ? 'approved' : 'pending';
};

const page = (c, h) =>
  h.render(c, VIEW, {
    ...chrome('design'), state: stateOf(c, h), states: STATES, base: '/design/approve',
  });

const approve = (c, h) => {
  h.session(c).data.designApproved = true;
  return h.render(c, `${VIEW}#gate`, { ...chrome('design'), state: 'approved' });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.approve = approve;
};
__modules["ui/views/stage_shell/build/run/run_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'build.run';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { run, findings } = __req("../../../../../services/facades/pipeline_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["red", "idle", "running", "green"];
const VIEW = 'ui/views/stage_shell/build/run/run_view.html';

const stateOf = (c, h) => {
  const q = c.req.query('state');
  if (STATES.includes(q)) return q;
  return h.session(c).data.buildState ?? 'red';
};

const bag = (state) => ({ ...chrome('build'), state, ...run(), ...findings() });

const page = (c, h) =>
  h.render(c, VIEW, { ...bag(stateOf(c, h)), states: STATES, base: '/build' });

// Run and Stop are the same route; the button says which. The prototype does
// not simulate a build — Run reaches `running` and stops there, because a fake
// green is the one outcome this surface must never show.
const submit = async (c, h) => {
  const action = (await h.form(c)).action;
  const state = action === 'stop' ? 'idle' : 'running';
  h.session(c).data.buildState = state;
  return h.render(c, `${VIEW}#panel`, bag(state));
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.submit = submit;
};
__modules["ui/views/stage_shell/build/finding/finding_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'build.finding';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { findings } = __req("../../../../../services/facades/pipeline_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["finding"];

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'finding';
  return h.render(c, 'ui/views/stage_shell/build/finding/finding_view.html', {
    ...chrome('build'), state, states: STATES, base: '/build/finding',
    ...findings(),
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
};
__modules["ui/views/stage_shell/build/approve/approve_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'build.approve';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["pending", "approved"];
const VIEW = 'ui/views/stage_shell/build/approve/approve_view.html';

// ?state= is the designer's override; otherwise the gate reports what the
// session records, so the swap and a later reload agree.
const stateOf = (c, h) => {
  const q = c.req.query('state');
  if (STATES.includes(q)) return q;
  return h.session(c).data.buildApproved ? 'approved' : 'pending';
};

const page = (c, h) =>
  h.render(c, VIEW, {
    ...chrome('build'), state: stateOf(c, h), states: STATES, base: '/build/approve',
  });

const approve = (c, h) => {
  h.session(c).data.buildApproved = true;
  return h.render(c, `${VIEW}#gate`, { ...chrome('build'), state: 'approved' });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.approve = approve;
};
__modules["ui/views/stage_shell/ship/targets/targets_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'ship.targets';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { targetList } = __req("../../../../../services/facades/catalog_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["targets"];

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'targets';
  return h.render(c, 'ui/views/stage_shell/ship/targets/targets_view.html', {
    ...chrome('ship'), state, states: STATES, base: '/ship',
    ...targetList(),
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
};
__modules["ui/views/stage_shell/ship/confirm/confirm_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'ship.confirm';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { release } = __req("../../../../../services/facades/pipeline_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["pending", "confirmed"];
const VIEW = 'ui/views/stage_shell/ship/confirm/confirm_view.html';

const stateOf = (c, h) => {
  const q = c.req.query('state');
  if (STATES.includes(q)) return q;
  return h.session(c).data.released ? 'confirmed' : 'pending';
};

const page = (c, h) =>
  h.render(c, VIEW, {
    ...chrome('ship'), state: stateOf(c, h), states: STATES, base: '/ship/confirm',
    ...release(),
  });

// The checkbox is `required`, but a required input is a client-side courtesy —
// it is absent from the body of anyone who posts around the form. The gate
// re-asserts it rather than trusting the markup.
const approve = async (c, h) => {
  const body = await h.form(c);
  if (!body.ack) {
    return h.render(c, `${VIEW}#gate`,
      { ...chrome('ship'), state: 'pending', ...release() }, 422);
  }
  h.session(c).data.released = true;
  return h.render(c, `${VIEW}#gate`,
    { ...chrome('ship'), state: 'confirmed', ...release() });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.approve = approve;
};
__modules["ui/views/stage_shell/chat/home/home_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'chat.home';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { transcript } = __req("../../../../../services/facades/chat_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["idle", "streaming", "tool-call"];
const VIEW = 'ui/views/stage_shell/chat/home/home_view.html';

const sentIn = (c, h) => (h.session(c).data.sent ??= []);

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'idle';
  return h.render(c, VIEW, {
    ...chrome('chat'), state, states: STATES, base: '/chat',
    ...transcript(), sent: sentIn(c, h),
  });
};

// The message lands in the session before the panel renders, so the swap and a
// later reload show the same transcript. No reply is faked: app_box answering
// is the pipeline's job, and a prototype that invents one is lying on the
// surface whose whole promise is "changes land as edits, never as claims".
const submit = async (c, h) => {
  const body = String((await h.form(c)).body ?? '').trim();
  if (body) sentIn(c, h).push(body);
  return h.render(c, `${VIEW}#panel`, {
    ...chrome('chat'), state: 'idle', ...transcript(), sent: sentIn(c, h),
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
__exports.submit = submit;
};
__modules["ui/views/stage_shell/settings/credentials/credentials_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'settings.credentials';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");


// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["credentials"];

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'credentials';
  return h.render(c, 'ui/views/stage_shell/settings/credentials/credentials_view.html', {
    ...chrome('settings'), state, states: STATES, base: '/settings',
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
};
__modules["ui/views/stage_shell/settings/devices/devices_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'settings.devices';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { deviceList } = __req("../../../../../services/facades/catalog_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["paired", "revoke"];

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'paired';
  return h.render(c, 'ui/views/stage_shell/settings/devices/devices_view.html', {
    ...chrome('settings'), state, states: STATES, base: '/settings/devices',
    ...deviceList(),
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
};
__modules["ui/views/stage_shell/settings/kits/kits_viewmodel.js"] = function (__exports, __req) {
const surfaceId = 'settings.kits';

const { chrome } = __req("../../../../../services/facades/shell_facade.js");
const { kitList } = __req("../../../../../services/facades/catalog_facade.js");

// States named in the brief are server-driven in-page variants, not forks.
const STATES = ["kits"];

const page = (c, h) => {
  const q = c.req.query('state');
  const state = STATES.includes(q) ? q : 'kits';
  return h.render(c, 'ui/views/stage_shell/settings/kits/kits_view.html', {
    ...chrome('settings'), state, states: STATES, base: '/settings/kits',
    ...kitList(),
  });
};

__exports.surfaceId = surfaceId;
__exports.page = page;
};
__modules["services/facades/shell_facade.js"] = function (__exports, __req) {
// ShellFacade — the chrome every surface renders inside.
// The four pipeline stages are a real sequence and carry their ordinal; chat
// and settings are utilities and deliberately do not.
const pipeline = __req("../repositories/pipeline_repository.js");

const UTIL = [
  { id: 'chat',     label: 'Chat',     href: '/chat' },
  { id: 'settings', label: 'Settings', href: '/settings' },
];
const HREF = { projects: '/', design: '/design', build: '/build', ship: '/ship' };

const chrome = (tab) => ({
  tab,
  stages: pipeline.stages().map((s) => ({ ...s, href: HREF[s.id] })),
  utils: UTIL,
});

__exports.chrome = chrome;
};
__modules["services/facades/project_facade.js"] = function (__exports, __req) {
// ProjectFacade — composes repositories into view-ready context.
const projects = __req("../repositories/project_repository.js");

// The seed stores platform IDENTIFIERS, because that is what the pipeline
// reads. Presentation names belong here, not in the seed and not in the view.
const PLATFORM = { ios: 'iOS', android: 'Android', macos: 'macOS',
                   windows: 'Windows', linux: 'Linux', web: 'Web', pwa: 'PWA' };

const list = () => {
  const all = projects.projects().map((p) => ({
    ...p, platforms: p.targets.map((t) => PLATFORM[t] || t).join(', '),
  }));
  return { projects: all, count: all.length };
};

__exports.list = list;
};
__modules["services/facades/pipeline_facade.js"] = function (__exports, __req) {
// PipelineFacade — build run, findings and the release triple.
const pipeline = __req("../repositories/pipeline_repository.js");

const run      = () => ({ log: pipeline.log() });
const findings = () => ({ findings: pipeline.findings(), finding: pipeline.findings()[0] });
const release  = () => ({ release: pipeline.release() });

__exports.run = run;
__exports.findings = findings;
__exports.release = release;
};
__modules["services/facades/catalog_facade.js"] = function (__exports, __req) {
// CatalogFacade — kits, devices and ship targets. One facade because all
// three answer the same question: what is actually available to this project.
const kits = __req("../repositories/kit_repository.js");
const devices = __req("../repositories/device_repository.js");
const targets = __req("../repositories/target_repository.js");

// Phase counts and stub findings are two separate axes and neither is derived
// from the other: a kit can be phase `stable` and still ship a provider that
// throws (auth, deploy). Deriving one from the other produces a number that
// silently stops matching the rows.
const kitList = () => ({ kits: kits.kits(), total: kits.total(), phases: kits.phases() });
const deviceList = () => ({ devices: devices.devices() });
const targetList = () => ({ targets: targets.targets() });

__exports.kitList = kitList;
__exports.deviceList = deviceList;
__exports.targetList = targetList;
};
__modules["services/facades/chat_facade.js"] = function (__exports, __req) {
// ChatFacade — the transcript.
const chat = __req("../repositories/chat_repository.js");

const transcript = () => ({ messages: chat.messages() });

__exports.transcript = transcript;
};
__modules["services/repositories/pipeline_repository.js"] = function (__exports, __req) {
// PipelineRepository — reads one fixture. Repositories are the DB-swap
// seam: a real backend replaces the body of this file and nothing above
// it changes.
const { readFixture } = __req("./fixture_reader.js");

const data = () => readFixture('../../models/pipeline_model/pipeline_fixtures.json');

const stages = () => data().stages;
const log = () => data().log;
const findings = () => data().findings;
const release = () => data().release;

__exports.stages = stages;
__exports.log = log;
__exports.findings = findings;
__exports.release = release;
};
__modules["services/repositories/project_repository.js"] = function (__exports, __req) {
// ProjectRepository — reads one fixture. Repositories are the DB-swap
// seam: a real backend replaces the body of this file and nothing above
// it changes.
const { readFixture } = __req("./fixture_reader.js");

const data = () => readFixture('../../models/project_model/project_fixtures.json');

const projects = () => data().projects;

__exports.projects = projects;
};
__modules["services/repositories/kit_repository.js"] = function (__exports, __req) {
// KitRepository — reads one fixture. Repositories are the DB-swap
// seam: a real backend replaces the body of this file and nothing above
// it changes.
const { readFixture } = __req("./fixture_reader.js");

const data = () => readFixture('../../models/kit_model/kit_fixtures.json');

const kits = () => data().kits;
const total = () => data().total;
const phases = () => data().phases;

__exports.kits = kits;
__exports.total = total;
__exports.phases = phases;
};
__modules["services/repositories/device_repository.js"] = function (__exports, __req) {
// DeviceRepository — reads one fixture. Repositories are the DB-swap
// seam: a real backend replaces the body of this file and nothing above
// it changes.
const { readFixture } = __req("./fixture_reader.js");

const data = () => readFixture('../../models/device_model/device_fixtures.json');

const devices = () => data().devices;

__exports.devices = devices;
};
__modules["services/repositories/target_repository.js"] = function (__exports, __req) {
// TargetRepository — reads one fixture. Repositories are the DB-swap
// seam: a real backend replaces the body of this file and nothing above
// it changes.
const { readFixture } = __req("./fixture_reader.js");

const data = () => readFixture('../../models/target_model/target_fixtures.json');

const targets = () => data().targets;

__exports.targets = targets;
};
__modules["services/repositories/chat_repository.js"] = function (__exports, __req) {
// ChatRepository — reads one fixture. Repositories are the DB-swap
// seam: a real backend replaces the body of this file and nothing above
// it changes.
const { readFixture } = __req("./fixture_reader.js");

const data = () => readFixture('../../models/chat_model/chat_fixtures.json');

const messages = () => data().messages;

__exports.messages = messages;
};
__modules["services/repositories/fixture_reader.js"] = function (__exports, __req) {
// Shared fixture reader — the only file that touches models/ JSON on disk.
// Repositories build on this; the productionize DB swap happens behind them.
const { readFileSync } = __req("node:fs");

const cache = new Map();

function readFixture(relFromThisFile) {
  if (!cache.has(relFromThisFile)) {
    cache.set(relFromThisFile, JSON.parse(readFileSync(new URL(relFromThisFile, "file://__ARTIFACT__/services/repositories/fixture_reader.js"), 'utf8')));
  }
  return cache.get(relFromThisFile);
}

__exports.readFixture = readFixture;
};
__modules["__virtual__:node:fs"] = function (__exports, __req) {
__exports.readFileSync = function (p, enc) {
  return globalThis.__readFileSync(typeof p === 'string' ? p : (p && p.href) || String(p));
};
};
  globalThis.__artifact = __load("app.routes.js");
})(globalThis);
