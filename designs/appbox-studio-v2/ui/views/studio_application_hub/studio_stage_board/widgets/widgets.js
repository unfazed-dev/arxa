// Barrel for the stage board's widgets. Named for the folder's artifact
// category exactly as showcase does (`widgets.dart`) — never `index`. Q-v2-4.
export { default as StageBoard } from './stage_board.tsx';
export { default as StageCard } from './stage_card.tsx';
