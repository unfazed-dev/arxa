// Barrel for the startup surface's widgets. Named for the folder's artifact
// category exactly as showcase does (`widgets.dart`) — never `index`. Q-v2-4.
export { default as BootChecklist } from './boot_checklist.tsx';
export { default as ProceedTrigger } from './proceed_trigger.tsx';
