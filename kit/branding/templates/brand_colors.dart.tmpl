// GENERATED by branding/tools/generate_branding.sh — DO NOT EDIT BY HAND.
// Source: appbox_kit/core/lib/common/appbox_kit_colors.dart (AppBoxKitColors / AppBoxKitDarkColors).
//
// The brand accent is seeded from the HOST's app_colors.dart (kcPrimaryColor);
// every other constant mirrors the kit ramp so host views can reference brand
// colors without a appbox_kit import. The theme BUILDER (kitLightTheme /
// kitDarkTheme) is intentionally NOT copied — it stays imported via
// package:ui_library; only the color CONSTANTS are vendored. Regenerating each
// pipeline run keeps this drift-free with appbox_kit_colors.dart.
//
// Regenerate: run the pipeline branding stage, or tools/generate_branding.sh.
import 'package:flutter/material.dart';

/// Local brand color surface (light). Mirrors AppBoxKitColors; accent is host-branded.
abstract final class BrandColors {
  static const accent = Color(0x{{ACCENT_ARGB}}); // = kcPrimaryColor (host app_colors.dart)
  static const surface = Color(0xFFF5F0E8);
  static const surface2 = Color(0xFFEAE3D6);
  static const paper = Color(0xFFFBF8F2);
  static const ink = Color(0xFF2D2419);
  static const ink2 = Color(0xFF3A3530);
  static const muted = Color(0xFF6E6760);
  static const faint = Color(0xFFA39A8E);
  static const rule = Color(0xFFD8CFBE);
  static const good = Color(0xFF4A7C3A);
  static const warn = Color(0xFFC68D2E);
  static const danger = Color(0xFFFF3B30);
  static const onAccent = Color(0xFFFBF8F2);
}

/// Local brand color surface (dark). Mirrors AppBoxKitDarkColors.
abstract final class BrandDarkColors {
  static const bone = Color(0xFF1C1814);
  static const bone2 = Color(0xFF262019);
  static const bone3 = Color(0xFF322A21);
  static const paper = Color(0xFF221D17);
  static const ink = Color(0xFFE8DCC4);
  static const ink2 = Color(0xFFD8CFBE);
  static const ink3 = Color(0xFF9C9384);
  static const ink4 = Color(0xFF948B7C);
  static const rule = Color(0xFF3A3328);
  static const danger = Color(0xFFE54E40);
  static const warn = Color(0xFFD9A547);
  static const good = Color(0xFF6FA85A);
}
