# arxa_kit_ui_library — COMPONENTS.md

Machine-readable inventory of the public surface in `lib/widgets/` and
`lib/services/`. Generated by reading the sources (class declarations + doc
comments) — when a widget's tiers change, update its row and its doc comment
together. Tiers name the real engine; "—" means no separate tier (the widget
is pure Flutter on that platform).

The canonical tier matrix + stock-widget ban list lives in
[`../core/NATIVE_COMPONENTS.md`](../core/NATIVE_COMPONENTS.md) — read it
before reaching for any primitive; this file is the inventory, that file is
the law.

## Widgets (`lib/widgets/`)

| Name | File | One-line purpose | iOS tier | Android tier | Flutter fallback |
|---|---|---|---|---|---|
| `ArxaKitNativeTabBar` (+`ArxaKitTab`) | `arxa_kit_tab_bar.dart` | Adaptive bottom tab bar; `ArxaKitTab` is one tab primitive (glyph → icon + SF Symbol) | `CNTabBar` (real Liquid Glass, iOS 26) | `NavigationBarM3E` (selection-pill morph) | `CupertinoTabBar` (Apple <26) / `NavigationBar` (desktop/web) |
| `ArxaKitBottomNavScaffold` (+`ArxaKitExtendBodyFabLift`) | `arxa_kit_bottom_nav_scaffold.dart` | Fully-automated bottom-nav scaffold — owns the index, lazy tab stack, `ArxaKitNativeTabBar`, `extendBody: true` | inherits `ArxaKitNativeTabBar` | inherits `ArxaKitNativeTabBar` | `Scaffold` composition |
| `ArxaKitDirectionalTabTransition` | `arxa_kit_directional_tab_transition.dart` | Direction-aware slide+fade for `StackedTabsRouter` tab switches — pure-Flutter tab bodies only (fade ghosts platform views; use `ArxaKitAnimatedTabStack` over native chrome) | — | — | pure Flutter |
| `ArxaKitTabSwitchTransition` | `arxa_kit_tab_switch_transition.dart` | Entrance-only direction-aware slide for kept-alive `IndexedStack` tabs — the fallback for stacks you don't own (superseded by `ArxaKitAnimatedTabStack`; no router animation, fade opt-in, off by default for platform-view safety) | — | — | pure Flutter |
| `ArxaKitAnimatedTabStack` | `arxa_kit_animated_tab_stack.dart` | Kept-alive lazy tab stack with a PAIRED direction-aware switch — the outgoing tab's live element slides out (router-safe per-slot `GlobalKey` reparenting, zero duplicate inflation) while the incoming slides in; fade opt-in, off by default (gate 1c2) | — | — | pure Flutter |
| `ArxaKitChip` | `arxa_kit_chip.dart` | Icon+label stadium chip for capability rails (content, no native tier) | — | — | pure Flutter |
| `ArxaKitChipCarousel` | `arxa_kit_chip_carousel.dart` | Horizontal chip rail — scrolling, optional snapping, overflow edge fades (content, no native tier) | — | — | pure Flutter |
| `ArxaKitDrawer` (+`ArxaKitDrawerVariant`) | `arxa_kit_drawer.dart` | Slide-over drawer on stock `Drawer` machinery — `glassPeek` (85% peek, large rounded trailing corner, `ArxaKitFrostedSurface` skin) / `plain` (stock themed); optional `motionDriver` (`KitMotionScope`) seam for gesture-driven content choreography (content, no native tier) | — | — | pure Flutter |
| `ArxaKitFabMorph` | `arxa_kit_fab_morph.dart` | Round M3E FAB that corner-radius shape-morphs on press (the morph `FabM3E` lacks) | — | M3E motion-token morph (Material `IconButton`-based) | itself (pure Flutter) |
| `ArxaKitFrostedSurface` | `arxa_kit_frosted_surface.dart` | Flutter-drawn frosted-glass surface (blur + saturation + tint + rim) — the content/sheet/dialog tier of ADR 0010. **Auto-opaque law:** a fully opaque tint takes the no-saveLayer branch on its own (opacity is a mode switch — never animate tint alpha across 1.0). Both branches publish `ArxaKitGlassLuminance` | — | — | pure Flutter |
| `ArxaKitGlassLuminance` | `arxa_kit_glass_luminance.dart` | Inherited luminance declaration — a surface publishes `opaque` + `brightness`; glass-styled native buttons on a bright OPAQUE base auto-demote to the filled-gray idiom with on-surface monochrome ink (the 2026-08-16 washout remedy, global). Fallback with no scope: the theme scaffold background. `luminanceAdaptive: false` on the button wrappers is the escape hatch | scopes the CN button tiers | n/a (M3E maps by emphasis) | pure Flutter scope |
| `ArxaKitOpaqueBarBase` | `arxa_kit_opaque_bar_base.dart` | Opaque pinned-strip base — plain native anchor (LiquidGlassContainer, renders nothing) + platform-view-safe opaque fill (tint token at alpha 1.0). The exact backing `ArxaKitNativeInputBar` paints; compose it under ANY docked strip (pending-attachment rows, accessory bars) so scrolled content never shows through the seam | native anchor only (clear plain effect; degrades to bare child off Apple-glass tiers) | n/a (anchor degrades) | opaque fill (no BackdropFilter — platform-view-safe by contract) |
| `ArxaKitGlassCard` | `arxa_kit_glass_card.dart` | Adaptive glass/card content surface (`wantNative` opt-out) | `LiquidGlassContainer` (iOS 26, gated on `supportsLiquidGlass`) | `ArxaKitFrostedSurface` (ADR 0010 frosted tier) | `ArxaKitFrostedSurface` |
| `ArxaKitImage` | `arxa_kit_image.dart` | Bundled-asset image with token-styled placeholder fallback | — (content, no native tier by design) | — | pure Flutter |
| `ArxaKitLazyIndexedStack` | `arxa_kit_lazy_indexed_stack.dart` | Lazy keep-alive index switch — builds on first visit, keeps offstage after | — | — | pure Flutter |
| `ArxaKitListSection` (+`ArxaKitListTile`) | `arxa_kit_list_section.dart` / `arxa_kit_list_tile.dart` | Grouped inset list sections + glyph-leading rows (settings / menu / plugin idioms; content, no native tier) — composes `ArxaKitGlassCard` | — | — | pure Flutter |
| `ArxaKitMenuItem` | `arxa_kit_menu_item.dart` | One item in a native menu — label + optional glyph primitives | (data class) | (data class) | (data class) |
| `ArxaKitNativeAppBar` | `arxa_kit_native_app_bar.dart` | Adaptive top app bar (implements `PreferredSizeWidget`) | `CupertinoNavigationBar` | `AppBarM3E` | `AppBar` / `CupertinoNavigationBar` |
| `ArxaKitNativeButton` (+`ArxaKitButtonStyle`) | `arxa_kit_native_button.dart` | Adaptive CTA button | `CNButton` (glass on iOS 26, `CupertinoButton` older) | `ButtonM3E` | `FilledButton` / `CupertinoButton` |
| `ArxaKitNativeChromeGate` (+`ArxaKitChromeHideMode`) | `arxa_kit_native_chrome_gate.dart` | Dematerializes a native-chrome child's pixels while a modal overlays it (fade+scale / unmount swap) | guards platform views | no-op in practice (in-layer composites) | pure Flutter |
| `ArxaKitNativeFab` | `arxa_kit_native_fab.dart` | Adaptive floating-action button | prominent-glass `CNButton` (HIG has no FAB) | `FabM3E` / `ExtendedFabM3E` | `FloatingActionButton` |
| `ArxaKitNativeFabMenu` | `arxa_kit_native_fab_menu.dart` | Adaptive FAB that morphs into a menu | glass `CNPopupMenuButton` | `FabMenuM3E` (FAB→menu morph) | `FloatingActionButton` + menu |
| `ArxaKitNativeIconButton` | `arxa_kit_native_icon_button.dart` | Adaptive icon button | `CNButton.icon` (SF Symbol) | `IconButtonM3E` (press shape-morph) | `IconButton` |
| `ArxaKitNativeInputBar` | `arxa_kit_native_input_bar.dart` | Adaptive **multiline composer** (default; `multiline: false` restores single-line) — `ArxaKitNativeTextField` growing field + leading/trailing `ArxaKitNativeIconButton` slots + an **`above:` slot** docking a row (pending attachments, recording controls — one zone, one row at a time) INSIDE the anchored opaque backing — the same pinned-chrome layer as the bar, where the view slicer holds the fill (no luminance artifacts while scrolling); keyboard-riding, grows upward past the action row; **action taps never dismiss** — the whole bar (actions included) joins the input tap group, so `+`/mic keep the keyboard mid-composition (iOS Messages idiom; device-verified 2026-08-16). The messenger record idiom is showcase-host composition on top of the bar, not kit behavior: tap-to-toggle record lives ON the trailing `ArxaKitNativeIconButton` itself (mic ↔ stop glyph swap, animated natively by the SF Symbol replace transition when the icon name changes on the Apple tiers), the kit `arxa_kit_media` audio service, and its live audio controls (level/timer/cancel) docking in the `above` slot — taking over the chips' spot while the recorder runs | inherits `ArxaKitNativeTextField` (`TextField(axis: .vertical)` glass capsule, rounded-rect, `heightChanged` channel) / `ArxaKitNativeIconButton` | inherits (`TextFieldM3E` min/maxLines grow-then-scroll) | Material composition (min/maxLines) |
| `ArxaKitNativeLoadingIndicator` | `arxa_kit_native_loading_indicator.dart` | Adaptive indeterminate spinner | `CupertinoActivityIndicator` | `LoadingIndicatorM3E` (shape-cycle) | `CircularProgressIndicator` |
| `ArxaKitNativeNavigationRail` (+`ArxaKitRailDestination`) | `arxa_kit_native_navigation_rail.dart` | Adaptive navigation rail with in-place expand/collapse | kit-owned Cupertino-styled rail (no CN rail exists) | `NavigationRailM3E` | Material `NavigationRail` |
| `ArxaKitNativePopupMenu` | `arxa_kit_native_popup_menu.dart` | Adaptive popup menu (trigger + `ArxaKitMenuItem`s) | `CNPopupMenuButton` (popover / `UIMenu`) | Material `showMenu` (M3 styling, motion via theme) | Material `showMenu` (web — same tier as Android; CN's version probe throws there) |
| `ArxaKitNativeProgress` | `arxa_kit_native_progress.dart` | Adaptive progress indicator, `.linear` / `.circular` geometries | Material `LinearProgressIndicator` / `CupertinoActivityIndicator` | `LinearProgressIndicatorM3E` / `CircularProgressIndicatorM3E` | `LinearProgressIndicator` / `CircularProgressIndicator` |
| `ArxaKitNativeRangeSlider` | `arxa_kit_native_range_slider.dart` | Adaptive two-thumb range slider | `CNRangeSlider` (two real `UISlider` thumbs, iOS 26) | `RangeSliderM3E` | Material `RangeSlider` (Cupertino-styled on older Apple) |
| `ArxaKitNativeSearchBar` | `arxa_kit_native_search_bar.dart` | Adaptive search bar (`wantNative` structural gate) | `CNSearchBar` | Material `SearchBar` (M3 motion via theme) | `SearchBar` / `SearchAnchor` |
| `ArxaKitNativeSegmentedControl` | `arxa_kit_native_segmented_control.dart` | Adaptive segmented control | `CNSegmentedControl` (`UISegmentedControl`) | `ButtonGroupM3E` (connected button group) | `SegmentedButton` / `TabBar` |
| `arxaKitShowSheet<T>()` | `arxa_kit_native_sheet.dart` | Platform-adaptive modal sheet function — ONE body-sized presentation (opaque `platformViewSafe` frosted base by default, filled-gray native close button, kit grabber, geometry probe; caller `backgroundColor` opts out of glass) | `CNBottomSheet.showCupertino` (Flutter `showCupertinoSheet`) | M3-themed `showModalBottomSheet` | `showModalBottomSheet` |
| `arxaKitShowNativeDialog<T>()` | `arxa_kit_native_dialog.dart` | Platform-adaptive alert dialog function (iOS: `ArxaKitFrostedSurface` panel, bold title + gray message, stacked full-width actions; ADR 0011 item 2) | — (no CN alert widget; idiom reproduced Flutter-drawn) | M3 `AlertDialog` | `showDialog` |
| `ArxaKitNativeSlider` | `arxa_kit_native_slider.dart` | Adaptive slider | `CNSlider` | `SliderM3E` | `Slider` |
| `ArxaKitNativeSliverAppBar` | `arxa_kit_native_sliver_app_bar.dart` | Adaptive sliver app bar (first sliver of a `CustomScrollView`; inventory — not app chrome) | `CupertinoNavigationBar` (sliver) | `SliverAppBarM3E` | `SliverAppBar` |
| `ArxaKitNativeSplitButton` | `arxa_kit_native_split_button.dart` | Adaptive split button — action segment + chevron menu | `CNGlassButtonGroup` | `SplitButtonM3E` | `Row` + `PopupMenuButton` |
| `ArxaKitNativeSwitch` | `arxa_kit_native_switch.dart` | Adaptive toggle switch | `CNSwitch` (self-degrades glass → Material) | Material `Switch` (no M3E branch by design) | `Switch` |
| `ArxaKitNativeTextField` | `arxa_kit_native_textfield.dart` | Adaptive text field (two-way `TextEditingController`) | `CNTextField` (Liquid Glass capsule) | `TextFieldM3E` (vendored fork, focus shape-morph) | Material `TextField` (`wantNative: false`) |
| `ArxaKitNativeToolbar` (+`ArxaKitToolbarAction`) | `arxa_kit_native_toolbar.dart` | Adaptive toolbar; `ArxaKitToolbarAction` is one action primitive | `CNGlassButtonGroup` (glass pill) | `ToolbarM3E` (`ToolbarActionM3E`) | `AppBar` / `TopAppBar` |
| `ArxaKitInputTapBehavior` | `arxa_kit_input_tap_behavior.dart` | Per-input tap-outside keyboard dismissal — grouped `TextFieldTapRegion` (`abxInputTapGroupId`) running the two-tier dismisser; carried by every kit input (textfield, search bar, input bar), `dismissKeyboardOnOutsideTap: false` opts out | region wraps every tier incl. the CN platform view | same wrapper | pure Flutter wrapper |
| `ArxaKitScrollEdgeEffect` | `arxa_kit_scroll_edge_effect.dart` | iOS 26-style scroll edge effect — progressive blur/fade on content beneath pinned chrome (ADR 0010; chrome never hides) | — | — | pure Flutter |
| `ArxaKitScrollOcclusionGate` | `arxa_kit_scroll_occlusion_gate.dart` | Residual blur-over-glass guard (ADR 0010 part 3); scroll duty superseded by `ArxaKitScrollEdgeEffect` | guards platform views | no-op in practice | pure Flutter |
| `ArxaKitStreamBuilder<T>` | `arxa_kit_stream_builder.dart` | `StreamBuilder` with rxdart `ValueStream` initial-data resolution | — | — | pure Flutter |

## Services (`lib/services/`)

| Name | File | One-line purpose | iOS tier | Android tier | Flutter fallback |
|---|---|---|---|---|---|
| `ArxaKitNavigationControllerService` | `navigation/arxa_kit_navigation_controller_service.dart` | Push navigation over `RouterService` — `CupertinoPageRoute` on native, route-based on web | `CupertinoPageRoute` | `CupertinoPageRoute` | `RouterService.navigateTo` (web) |
| `ArxaKitPlatformPages` (+`ArxaKitPlatformPagesMixin`) | `navigation/arxa_kit_platform_pages.dart` | Per-platform native page wrapping for stacked Navigator 2.0 (edge-swipe-back / predictive-back) | cupertino page (edge-swipe-back) | material page (predictive-back) | stock `AdaptivePage` |
| `ArxaKitNotificationService` (+`ArxaKitNotificationKind`, `ArxaKitToastPosition`) | `notifications/arxa_kit_notification_service.dart` | In-app toast/snackbar with severity + position | `CNToast.show` / `.success` / `.error` | kit M3E snackbar via `SnackbarService` | `showSnackBar` |
| `ArxaKitBottomSheetService` | `sheet/arxa_kit_bottom_sheet_service.dart` | Drop-in `BottomSheetService` presenting through `arxaKitShowSheet` | `CNBottomSheet` | M3-themed `showModalBottomSheet` | `showModalBottomSheet` |

---

Ban matrix (which stock widgets the gates reject, and the `ArxaKitNative*` that
replaces each): [`../core/NATIVE_COMPONENTS.md`](../core/NATIVE_COMPONENTS.md).
Test doubles for the services above: `lib/arxa_kit_testing.dart`
(`FakeArxaKitNotificationService`, `FakeArxaKitBottomSheetService`,
`FakeArxaKitNavigationControllerService`).
