# appbox_kit_ui_library — COMPONENTS.md

Machine-readable inventory of the public surface in `lib/widgets/` and
`lib/services/`. Generated by reading the sources (class declarations + doc
comments) — when a widget's tiers change, update its row and its doc comment
together. Tiers name the real engine; "—" means no separate tier (the widget
is pure Flutter on that platform).

The canonical tier matrix + stock-widget ban list lives in
[`../core/NATIVE_COMPONENTS.md`](../core/NATIVE_COMPONENTS.md) — read it
before reaching for any primitive; this file is the inventory, that file is
the law.

## Widgets (`lib/widgets/`)

| Name | File | One-line purpose | iOS tier | Android tier | Flutter fallback |
|---|---|---|---|---|---|
| `AppBoxKitNativeTabBar` (+`AppBoxKitTab`) | `appbox_kit_tab_bar.dart` | Adaptive bottom tab bar; `AppBoxKitTab` is one tab primitive (glyph → icon + SF Symbol) | `CNTabBar` (real Liquid Glass, iOS 26) | `NavigationBarM3E` (selection-pill morph) | `CupertinoTabBar` (Apple <26) / `NavigationBar` (desktop/web) |
| `AppBoxKitBottomNavScaffold` (+`AppBoxKitExtendBodyFabLift`) | `appbox_kit_bottom_nav_scaffold.dart` | Fully-automated bottom-nav scaffold — owns the index, lazy tab stack, `AppBoxKitNativeTabBar`, `extendBody: true` | inherits `AppBoxKitNativeTabBar` | inherits `AppBoxKitNativeTabBar` | `Scaffold` composition |
| `AppBoxKitDirectionalTabTransition` | `appbox_kit_directional_tab_transition.dart` | Direction-aware slide+fade for `StackedTabsRouter` tab switches — pure-Flutter tab bodies only (fade ghosts platform views; use `AppBoxKitAnimatedTabStack` over native chrome) | — | — | pure Flutter |
| `AppBoxKitTabSwitchTransition` | `appbox_kit_tab_switch_transition.dart` | Entrance-only direction-aware slide for kept-alive `IndexedStack` tabs — the fallback for stacks you don't own (superseded by `AppBoxKitAnimatedTabStack`; no router animation, fade opt-in, off by default for platform-view safety) | — | — | pure Flutter |
| `AppBoxKitAnimatedTabStack` | `appbox_kit_animated_tab_stack.dart` | Kept-alive lazy tab stack with a PAIRED direction-aware switch — the outgoing tab's live element slides out (router-safe per-slot `GlobalKey` reparenting, zero duplicate inflation) while the incoming slides in; fade opt-in, off by default (gate 1c2) | — | — | pure Flutter |
| `AppBoxKitChip` | `appbox_kit_chip.dart` | Icon+label stadium chip for capability rails (content, no native tier) | — | — | pure Flutter |
| `AppBoxKitChipCarousel` | `appbox_kit_chip_carousel.dart` | Horizontal chip rail — scrolling, optional snapping, overflow edge fades (content, no native tier) | — | — | pure Flutter |
| `AppBoxKitDrawer` (+`AppBoxKitDrawerVariant`) | `appbox_kit_drawer.dart` | Slide-over drawer on stock `Drawer` machinery — `glassPeek` (85% peek, large rounded trailing corner, `AppBoxKitFrostedSurface` skin) / `plain` (stock themed); optional `motionDriver` (`KitMotionScope`) seam for gesture-driven content choreography (content, no native tier) | — | — | pure Flutter |
| `AppBoxKitFabMorph` | `appbox_kit_fab_morph.dart` | Round M3E FAB that corner-radius shape-morphs on press (the morph `FabM3E` lacks) | — | M3E motion-token morph (Material `IconButton`-based) | itself (pure Flutter) |
| `AppBoxKitFrostedSurface` | `appbox_kit_frosted_surface.dart` | Flutter-drawn frosted-glass surface (blur + saturation + tint + rim) — the content/sheet/dialog tier of ADR 0010 | — | — | pure Flutter |
| `AppBoxKitOpaqueBarBase` | `appbox_kit_opaque_bar_base.dart` | Opaque pinned-strip base — plain native anchor (LiquidGlassContainer, renders nothing) + platform-view-safe opaque fill (tint token at alpha 1.0). The exact backing `AppBoxKitNativeInputBar` paints; compose it under ANY docked strip (pending-attachment rows, accessory bars) so scrolled content never shows through the seam | native anchor only (clear plain effect; degrades to bare child off Apple-glass tiers) | n/a (anchor degrades) | opaque fill (no BackdropFilter — platform-view-safe by contract) |
| `AppBoxKitGlassCard` | `appbox_kit_glass_card.dart` | Adaptive glass/card content surface (`wantNative` opt-out) | `LiquidGlassContainer` (iOS 26, gated on `supportsLiquidGlass`) | `AppBoxKitFrostedSurface` (ADR 0010 frosted tier) | `AppBoxKitFrostedSurface` |
| `AppBoxKitImage` | `appbox_kit_image.dart` | Bundled-asset image with token-styled placeholder fallback | — (content, no native tier by design) | — | pure Flutter |
| `AppBoxKitLazyIndexedStack` | `appbox_kit_lazy_indexed_stack.dart` | Lazy keep-alive index switch — builds on first visit, keeps offstage after | — | — | pure Flutter |
| `AppBoxKitListSection` (+`AppBoxKitListTile`) | `appbox_kit_list_section.dart` / `appbox_kit_list_tile.dart` | Grouped inset list sections + glyph-leading rows (settings / menu / plugin idioms; content, no native tier) — composes `AppBoxKitGlassCard` | — | — | pure Flutter |
| `AppBoxKitMenuItem` | `appbox_kit_menu_item.dart` | One item in a native menu — label + optional glyph primitives | (data class) | (data class) | (data class) |
| `AppBoxKitNativeAppBar` | `appbox_kit_native_app_bar.dart` | Adaptive top app bar (implements `PreferredSizeWidget`) | `CupertinoNavigationBar` | `AppBarM3E` | `AppBar` / `CupertinoNavigationBar` |
| `AppBoxKitNativeButton` (+`AppBoxKitButtonStyle`) | `appbox_kit_native_button.dart` | Adaptive CTA button | `CNButton` (glass on iOS 26, `CupertinoButton` older) | `ButtonM3E` | `FilledButton` / `CupertinoButton` |
| `AppBoxKitNativeChromeGate` (+`AppBoxKitChromeHideMode`) | `appbox_kit_native_chrome_gate.dart` | Dematerializes a native-chrome child's pixels while a modal overlays it (fade+scale / unmount swap) | guards platform views | no-op in practice (in-layer composites) | pure Flutter |
| `AppBoxKitNativeFab` | `appbox_kit_native_fab.dart` | Adaptive floating-action button | prominent-glass `CNButton` (HIG has no FAB) | `FabM3E` / `ExtendedFabM3E` | `FloatingActionButton` |
| `AppBoxKitNativeFabMenu` | `appbox_kit_native_fab_menu.dart` | Adaptive FAB that morphs into a menu | glass `CNPopupMenuButton` | `FabMenuM3E` (FAB→menu morph) | `FloatingActionButton` + menu |
| `AppBoxKitNativeIconButton` | `appbox_kit_native_icon_button.dart` | Adaptive icon button | `CNButton.icon` (SF Symbol) | `IconButtonM3E` (press shape-morph) | `IconButton` |
| `AppBoxKitNativeInputBar` | `appbox_kit_native_input_bar.dart` | Adaptive **multiline composer** (default; `multiline: false` restores single-line) — `AppBoxKitNativeTextField` growing field + leading/trailing `AppBoxKitNativeIconButton` slots + an **`above:` slot** docking a row (pending attachments) INSIDE the anchored opaque backing — the same pinned-chrome layer as the bar, where the view slicer holds the fill (no luminance artifacts while scrolling); keyboard-riding, grows upward past the action row; **action taps never dismiss** — the whole bar (actions included) joins the input tap group, so `+`/mic keep the keyboard mid-composition (iOS Messages idiom; device-verified 2026-08-16). The messenger record idiom is showcase-host composition on top of the bar, not kit behavior: tap-to-toggle record lives ON the trailing `AppBoxKitNativeIconButton` itself (mic ↔ stop glyph swap, animated natively by the SF Symbol replace transition when the icon name changes on the Apple tiers) and the kit `appbox_kit_media` audio service | inherits `AppBoxKitNativeTextField` (`TextField(axis: .vertical)` glass capsule, rounded-rect, `heightChanged` channel) / `AppBoxKitNativeIconButton` | inherits (`TextFieldM3E` min/maxLines grow-then-scroll) | Material composition (min/maxLines) |
| `AppBoxKitNativeLoadingIndicator` | `appbox_kit_native_loading_indicator.dart` | Adaptive indeterminate spinner | `CupertinoActivityIndicator` | `LoadingIndicatorM3E` (shape-cycle) | `CircularProgressIndicator` |
| `AppBoxKitNativeNavigationRail` (+`AppBoxKitRailDestination`) | `appbox_kit_native_navigation_rail.dart` | Adaptive navigation rail with in-place expand/collapse | kit-owned Cupertino-styled rail (no CN rail exists) | `NavigationRailM3E` | Material `NavigationRail` |
| `AppBoxKitNativePopupMenu` | `appbox_kit_native_popup_menu.dart` | Adaptive popup menu (trigger + `AppBoxKitMenuItem`s) | `CNPopupMenuButton` (popover / `UIMenu`) | Material `showMenu` (M3 styling, motion via theme) | Material `showMenu` (web — same tier as Android; CN's version probe throws there) |
| `AppBoxKitNativeProgress` | `appbox_kit_native_progress.dart` | Adaptive progress indicator, `.linear` / `.circular` geometries | Material `LinearProgressIndicator` / `CupertinoActivityIndicator` | `LinearProgressIndicatorM3E` / `CircularProgressIndicatorM3E` | `LinearProgressIndicator` / `CircularProgressIndicator` |
| `AppBoxKitNativeRangeSlider` | `appbox_kit_native_range_slider.dart` | Adaptive two-thumb range slider | `CNRangeSlider` (two real `UISlider` thumbs, iOS 26) | `RangeSliderM3E` | Material `RangeSlider` (Cupertino-styled on older Apple) |
| `AppBoxKitNativeSearchBar` | `appbox_kit_native_search_bar.dart` | Adaptive search bar (`wantNative` structural gate) | `CNSearchBar` | Material `SearchBar` (M3 motion via theme) | `SearchBar` / `SearchAnchor` |
| `AppBoxKitNativeSegmentedControl` | `appbox_kit_native_segmented_control.dart` | Adaptive segmented control | `CNSegmentedControl` (`UISegmentedControl`) | `ButtonGroupM3E` (connected button group) | `SegmentedButton` / `TabBar` |
| `appBoxKitShowNativeSheet<T>()` | `appbox_kit_native_sheet.dart` | Platform-adaptive modal bottom sheet function — iOS tier: transparent route + `AppBoxKitFrostedSurface` body + grabber + floating inset (geometry probe kept; caller `backgroundColor` opts out of glass) | `CNBottomSheet.show` | M3-themed `showModalBottomSheet` | `showModalBottomSheet` |
| `appBoxKitShowNativeDialog<T>()` | `appbox_kit_native_dialog.dart` | Platform-adaptive alert dialog function (iOS: `AppBoxKitFrostedSurface` panel, bold title + gray message, stacked full-width actions; ADR 0011 item 2) | — (no CN alert widget; idiom reproduced Flutter-drawn) | M3 `AlertDialog` | `showDialog` |
| `AppBoxKitNativeSlider` | `appbox_kit_native_slider.dart` | Adaptive slider | `CNSlider` | `SliderM3E` | `Slider` |
| `AppBoxKitNativeSliverAppBar` | `appbox_kit_native_sliver_app_bar.dart` | Adaptive sliver app bar (first sliver of a `CustomScrollView`; inventory — not app chrome) | `CupertinoNavigationBar` (sliver) | `SliverAppBarM3E` | `SliverAppBar` |
| `AppBoxKitNativeSplitButton` | `appbox_kit_native_split_button.dart` | Adaptive split button — action segment + chevron menu | `CNGlassButtonGroup` | `SplitButtonM3E` | `Row` + `PopupMenuButton` |
| `AppBoxKitNativeSwitch` | `appbox_kit_native_switch.dart` | Adaptive toggle switch | `CNSwitch` (self-degrades glass → Material) | Material `Switch` (no M3E branch by design) | `Switch` |
| `AppBoxKitNativeTextField` | `appbox_kit_native_textfield.dart` | Adaptive text field (two-way `TextEditingController`) | `CNTextField` (Liquid Glass capsule) | `TextFieldM3E` (vendored fork, focus shape-morph) | Material `TextField` (`wantNative: false`) |
| `AppBoxKitNativeToolbar` (+`AppBoxKitToolbarAction`) | `appbox_kit_native_toolbar.dart` | Adaptive toolbar; `AppBoxKitToolbarAction` is one action primitive | `CNGlassButtonGroup` (glass pill) | `ToolbarM3E` (`ToolbarActionM3E`) | `AppBar` / `TopAppBar` |
| `AppBoxKitInputTapBehavior` | `appbox_kit_input_tap_behavior.dart` | Per-input tap-outside keyboard dismissal — grouped `TextFieldTapRegion` (`abxInputTapGroupId`) running the two-tier dismisser; carried by every kit input (textfield, search bar, input bar), `dismissKeyboardOnOutsideTap: false` opts out | region wraps every tier incl. the CN platform view | same wrapper | pure Flutter wrapper |
| `AppBoxKitScrollEdgeEffect` | `appbox_kit_scroll_edge_effect.dart` | iOS 26-style scroll edge effect — progressive blur/fade on content beneath pinned chrome (ADR 0010; chrome never hides) | — | — | pure Flutter |
| `AppBoxKitScrollOcclusionGate` | `appbox_kit_scroll_occlusion_gate.dart` | Residual blur-over-glass guard (ADR 0010 part 3); scroll duty superseded by `AppBoxKitScrollEdgeEffect` | guards platform views | no-op in practice | pure Flutter |
| `AppBoxKitStreamBuilder<T>` | `appbox_kit_stream_builder.dart` | `StreamBuilder` with rxdart `ValueStream` initial-data resolution | — | — | pure Flutter |

## Services (`lib/services/`)

| Name | File | One-line purpose | iOS tier | Android tier | Flutter fallback |
|---|---|---|---|---|---|
| `AppBoxKitNavigationControllerService` | `navigation/appbox_kit_navigation_controller_service.dart` | Push navigation over `RouterService` — `CupertinoPageRoute` on native, route-based on web | `CupertinoPageRoute` | `CupertinoPageRoute` | `RouterService.navigateTo` (web) |
| `AppBoxKitPlatformPages` (+`AppBoxKitPlatformPagesMixin`) | `navigation/appbox_kit_platform_pages.dart` | Per-platform native page wrapping for stacked Navigator 2.0 (edge-swipe-back / predictive-back) | cupertino page (edge-swipe-back) | material page (predictive-back) | stock `AdaptivePage` |
| `AppBoxKitNotificationService` (+`AppBoxKitNotificationKind`, `AppBoxKitToastPosition`) | `notifications/appbox_kit_notification_service.dart` | In-app toast/snackbar with severity + position | `CNToast.show` / `.success` / `.error` | kit M3E snackbar via `SnackbarService` | `showSnackBar` |
| `AppBoxKitBottomSheetService` | `sheet/appbox_kit_bottom_sheet_service.dart` | Drop-in `BottomSheetService` presenting through `appBoxKitShowNativeSheet` | `CNBottomSheet` | M3-themed `showModalBottomSheet` | `showModalBottomSheet` |

---

Ban matrix (which stock widgets the gates reject, and the `AppBoxKitNative*` that
replaces each): [`../core/NATIVE_COMPONENTS.md`](../core/NATIVE_COMPONENTS.md).
Test doubles for the services above: `lib/appbox_kit_testing.dart`
(`FakeAppBoxKitNotificationService`, `FakeAppBoxKitBottomSheetService`,
`FakeAppBoxKitNavigationControllerService`).
