<Callout id="header" tone="info">

# appbox_kit_ui_library — Per-Package Playbook


`appbox_kit_ui_library` v**0.1.0** · `publish_to: 'none'` · status: **active**


**Role (one line):** Stacked kit UI tier — Kit* adaptive port widgets, UI-coupled services (navigation/sheet/notifications), overlay + component plumbing.


**Scope of this document:** lets a human OR an LLM use, wire, and extend `appbox_kit_ui_library` **without reading its source**. The public surface below was extracted from the barrel; README content is folded into the narrative sections.


## Table of contents

0. Header & TOC — *you are here* · 1. Overview & role · 2. Public surface · 3. Usage & wiring · 4. Integration map · 5. Architecture · 6. Gotchas · 7. Testing · 8. Features · 9. Decisions · 10. References · 11. Open questions


</Callout>


### Overview & role

Stacked kit UI tier — Kit* adaptive port widgets, UI-coupled services (navigation/sheet/notifications), overlay + component plumbing. Re-exports appbox_kit_core so consumers import one package.


### Public surface

Exported from `lib/appbox_kit_ui_library.dart`:

<Table id="appbox_kit_ui_library-api" columns={["Symbol", "Kind", "Defined in"]} rows={[["AppBoxKitNativeComponent", "enum", "enums/appbox_kit_native_component.dart"], ["AppBoxKitButtonType", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitButtonLoaderDuration", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitButtonLoaderState", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitIconButtonShape", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitAnimatedNavbarContainerHeightState", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitBlockPanelExpanded", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitBlockPanelAlignment", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitContainerExpandDirection", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitContainerExpansionLimit", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitContainerRetractButtonPosition", "enum", "enums/appbox_kit_widgets_enum.dart"], ["AppBoxKitAction", "class", "utils/kit_action/appbox_kit_action.dart"], ["AppBoxKitActionBuilder", "class", "utils/kit_action/appbox_kit_action_builder.dart"], ["AppBoxKitActionHub", "class", "utils/kit_action/appbox_kit_action_hub.dart"], ["AppBoxKitActionCommand", "class", "utils/kit_action/appbox_kit_action_hub.dart"], ["AppBoxKitRetryPolicy", "typedef", "utils/kit_action/appbox_kit_action_hub.dart"], ["AppBoxKitActionConfig", "class", "utils/kit_action/appbox_kit_action_config.dart"], ["AppBoxKitCancelledException", "class", "utils/kit_action/appbox_kit_action_types.dart"], ["AppBoxKitThrottledException", "class", "utils/kit_action/appbox_kit_action_types.dart"], ["AppBoxKitCancelToken", "class", "utils/kit_action/appbox_kit_action_types.dart"], ["AppBoxKitActionCancellable", "class", "utils/kit_action/appbox_kit_action_types.dart"], ["AppBoxKitSnackbarType", "enum", "utils/kit_action/appbox_kit_snackbar_type.dart"], ["AppBoxKitNotificationType", "enum", "utils/kit_action/appbox_kit_notification_type.dart"], ["AppBoxKitErrorManager", "class", "utils/kit_action/managers/appbox_kit_error_manager.dart"], ["AppBoxKitLoadingManager", "class", "utils/kit_action/managers/appbox_kit_loading_manager.dart"], ["AppBoxKitNotificationManager", "class", "utils/kit_action/managers/appbox_kit_notification_manager.dart"], ["AppBoxKitStreamManager", "class", "utils/kit_action/managers/appbox_kit_stream_manager.dart"], ["AppBoxKitSuccessManager", "class", "utils/kit_action/managers/appbox_kit_success_manager.dart"], ["AppBoxKitAsyncExecutor", "class", "utils/kit_action/executors/appbox_kit_async_executor.dart"], ["AppBoxKitActionExecutor", "class", "utils/kit_action/executors/appbox_kit_action_executor.dart"], ["AppBoxKitOverlayService", "class", "extensions/appbox_kit_overlay_extension.dart"], ["AppBoxKitOverlayOptions", "class", "extensions/appbox_kit_overlay_extension.dart"], ["AppBoxKitOverlayControlPosition", "enum", "extensions/appbox_kit_overlay_extension.dart"], ["AppBoxKitNavigationControllerService", "class", "services/navigation/appbox_kit_navigation_controller_service.dart"], ["AppBoxKitPlatformPages", "class", "services/navigation/appbox_kit_platform_pages.dart"], ["AppBoxKitPlatformPagesMixin", "mixin", "services/navigation/appbox_kit_platform_pages.dart"], ["AppBoxKitBottomSheetService", "class", "services/sheet/appbox_kit_bottom_sheet_service.dart"], ["AppBoxKitNotificationKind", "enum", "services/notifications/appbox_kit_notification_service.dart"], ["AppBoxKitToastPosition", "enum", "services/notifications/appbox_kit_notification_service.dart"], ["AppBoxKitNotificationService", "class", "services/notifications/appbox_kit_notification_service.dart"], ["AppBoxKitAnimatedTabStack", "class", "widgets/appbox_kit_animated_tab_stack.dart"], ["AppBoxKitExtendBodyFabLift", "class", "widgets/appbox_kit_bottom_nav_scaffold.dart"], ["AppBoxKitBottomNavScaffold", "class", "widgets/appbox_kit_bottom_nav_scaffold.dart"], ["AppBoxKitChip", "class", "widgets/appbox_kit_chip.dart"], ["AppBoxKitChipCarousel", "class", "widgets/appbox_kit_chip_carousel.dart"], ["AppBoxKitCodeBlock", "class", "widgets/appbox_kit_code_block.dart"], ["AppBoxKitDirectionalTabTransition", "class", "widgets/appbox_kit_directional_tab_transition.dart"], ["AppBoxKitDrawerVariant", "enum", "widgets/appbox_kit_drawer.dart"], ["AppBoxKitDrawer", "class", "widgets/appbox_kit_drawer.dart"], ["AppBoxKitFabMorph", "class", "widgets/appbox_kit_fab_morph.dart"], ["AppBoxKitFrostedSurface", "class", "widgets/appbox_kit_frosted_surface.dart"], ["AppBoxKitGlassCard", "class", "widgets/appbox_kit_glass_card.dart"], ["AppBoxKitImage", "class", "widgets/appbox_kit_image.dart"], ["AppBoxKitLazyIndexedStack", "class", "widgets/appbox_kit_lazy_indexed_stack.dart"], ["AppBoxKitListSection", "class", "widgets/appbox_kit_list_section.dart"], ["AppBoxKitListTile", "class", "widgets/appbox_kit_list_tile.dart"], ["AppBoxKitMarkdown", "class", "widgets/appbox_kit_markdown.dart"], ["AppBoxKitMenuItem", "class", "widgets/appbox_kit_menu_item.dart"], ["AppBoxKitNativeAppBar", "class", "widgets/appbox_kit_native_app_bar.dart"], ["AppBoxKitNativeButton", "class", "widgets/appbox_kit_native_button.dart"], ["AppBoxKitButtonStyle", "enum", "widgets/appbox_kit_native_button.dart"], ["AppBoxKitChromeHideMode", "enum", "widgets/appbox_kit_native_chrome_gate.dart"], ["AppBoxKitNativeChromeGate", "class", "widgets/appbox_kit_native_chrome_gate.dart"], ["AppBoxKitDialogActionRole", "enum", "widgets/appbox_kit_native_dialog.dart"], ["AppBoxKitNativeDialogAction", "class", "widgets/appbox_kit_native_dialog.dart"], ["AppBoxKitNativeFab", "class", "widgets/appbox_kit_native_fab.dart"], ["AppBoxKitNativeFabMenu", "class", "widgets/appbox_kit_native_fab_menu.dart"], ["AppBoxKitNativeIconButton", "class", "widgets/appbox_kit_native_icon_button.dart"], ["AppBoxKitNativeInputBar", "class", "widgets/appbox_kit_native_input_bar.dart"], ["AppBoxKitNativeLoadingIndicator", "class", "widgets/appbox_kit_native_loading_indicator.dart"], ["AppBoxKitRailDestination", "class", "widgets/appbox_kit_native_navigation_rail.dart"], ["AppBoxKitNativeNavigationRail", "class", "widgets/appbox_kit_native_navigation_rail.dart"], ["AppBoxKitNativePopupMenu", "class", "widgets/appbox_kit_native_popup_menu.dart"], ["AppBoxKitNativeProgress", "class", "widgets/appbox_kit_native_progress.dart"], ["AppBoxKitNativeRangeSlider", "class", "widgets/appbox_kit_native_range_slider.dart"], ["AppBoxKitNativeSearchBar", "class", "widgets/appbox_kit_native_search_bar.dart"], ["AppBoxKitNativeSegmentedControl", "class", "widgets/appbox_kit_native_segmented_control.dart"], ["AppBoxKitNativeSlider", "class", "widgets/appbox_kit_native_slider.dart"], ["AppBoxKitNativeSliverAppBar", "class", "widgets/appbox_kit_native_sliver_app_bar.dart"], ["AppBoxKitNativeSplitButton", "class", "widgets/appbox_kit_native_split_button.dart"], ["AppBoxKitNativeSwitch", "class", "widgets/appbox_kit_native_switch.dart"], ["AppBoxKitNativeTextField", "class", "widgets/appbox_kit_native_textfield.dart"], ["AppBoxKitToolbarAction", "class", "widgets/appbox_kit_native_toolbar.dart"], ["AppBoxKitNativeToolbar", "class", "widgets/appbox_kit_native_toolbar.dart"], ["AppBoxKitScrollEdge", "enum", "widgets/appbox_kit_scroll_edge_effect.dart"], ["AppBoxKitScrollEdgeEffectStyle", "enum", "widgets/appbox_kit_scroll_edge_effect.dart"], ["AppBoxKitScrollEdgeEffect", "class", "widgets/appbox_kit_scroll_edge_effect.dart"], ["AppBoxKitScrollOcclusionGate", "class", "widgets/appbox_kit_scroll_occlusion_gate.dart"], ["AppBoxKitScrollEdges", "enum", "widgets/appbox_kit_edge_aware_list_view.dart"], ["AppBoxKitEdgeAwareListView", "class", "widgets/appbox_kit_edge_aware_list_view.dart"], ["AppBoxKitEdgeAwareSliverList", "class", "widgets/appbox_kit_edge_aware_list_view.dart"], ["AppBoxKitTopEdgeScrim", "class", "widgets/appbox_kit_native_floating_bar.dart"], ["AppBoxKitBottomEdgeScrim", "class", "widgets/appbox_kit_native_floating_bar.dart"], ["AppBoxKitBottomEdgeScrimHost", "class", "widgets/appbox_kit_native_floating_bar.dart"], ["AppBoxKitStreamBuilder", "class", "widgets/appbox_kit_stream_builder.dart"], ["AppBoxKitSvg", "class", "widgets/appbox_kit_svg.dart"], ["AppBoxKitTab", "class", "widgets/appbox_kit_tab_bar.dart"], ["AppBoxKitNativeTabBar", "class", "widgets/appbox_kit_tab_bar.dart"], ["AppBoxKitTabSwitchTransition", "class", "widgets/appbox_kit_tab_switch_transition.dart"]]} />

Edge treatment is a property of the *scrollable*, not of each leaf: `AppBoxKitEdgeAwareListView`/`AppBoxKitEdgeAwareSliverList` apply `AppBoxKitScrollEdgeEffect` to every child, and their `edges:` knob takes `AppBoxKitScrollEdges` (`none`/`top`/`bottom`/`both`, defaulting to **`both`** — both edges treated unless you opt out). Note the one-character trap: `AppBoxKitScrollEdges` (plural, `edge_aware_list_view.dart`) is the per-scrollable selector; `AppBoxKitScrollEdge` (singular, `scroll_edge_effect.dart`) is the which-edge parameter of a single effect. The bottom edge additionally has a scrim family in `native_floating_bar.dart` — `AppBoxKitBottomEdgeScrim` (the gradient itself, `fadeExtent` defaulting to `kAppBoxKitFloatingBarBlockHeight`) and `AppBoxKitBottomEdgeScrimHost` (wraps a subtree, `enabled` defaulting to true), the counterpart to the existing `AppBoxKitTopEdgeScrim`. `AppBoxKitBottomNavScaffold` mounts the host for you via its `bottomEdgeScrim` param (default **true**).

Glass surfaces now default to *opaque*: `AppBoxKitGlassCard`, `AppBoxKitNativeInputBar`, and the dialog surface (both `appBoxKitShowNativeDialog` and the widget) all take `opaqueGlass` defaulting to **true**, painting a flat opaque base behind the content; pass `opaqueGlass: false` for the old fully transparent backing. Sheets are the exception worth reading twice — `appBoxKitShowNativeSheet(opaqueGlass:)` defaults to **true**, but the raw sheet widget's own field defaults to **false**, so direct widget construction is transparent unless you say otherwise. Rationale for all of these lives in `docs/liquid-glass-allowlist.md`.

Test doubles in `lib/appbox_kit_testing.dart`: `AppBoxKitNotificationRecord`, `FakeAppBoxKitNotificationService`, `AppBoxKitBottomSheetCall`, `AppBoxKitCustomSheetCall`, `FakeAppBoxKitBottomSheetService`, `AppBoxKitNavigationRecord`, `FakeAppBoxKitNavigationControllerService`.

Scripts and call-counts — tests never touch platform channels.


### Usage & wiring

Add `appbox_kit_ui_library` as a path dependency and import the single barrel `package:appbox_kit_ui_library/appbox_kit_ui_library.dart` — it re-exports `appbox_kit_core`, so widgets, tokens, the appBoxKitLocator, and the error/theme services all arrive through one import. The barrel also re-exports the appbox-mandated framework surface — `stacked` (+ `stacked_annotations`), rxdart's `BehaviorSubject`/`ValueStream`/`Rx`/`SwitchMapExtension`, and the stacked_services config types (`RouterService`, `SnackbarService`, `SnackbarConfig`, `SnackPosition`, `StackedService`) — so app pubspecs never declare those packages. At boot, call `setupLocator()` (registers `AppBoxKitNotificationService` and the rest), then `setupAppBoxKitUiServices()` (DialogService / SnackbarService / BottomSheetService-as-AppBoxKitBottomSheetService / Talker — keeps those registrations, and their packages, out of app code), then `setupKitSnackbars()` to install the `SnackbarConfig` variants the Android and iOS-action-fallback tiers render. Mix `AppBoxKitPlatformPagesMixin` into your generated `AppRouter` (which already extends `StackedRouterWeb`) to render Cupertino pages on iOS and Material pages elsewhere. Widget call sites take primitives only — e.g. `AppBoxKitNativeButton(onPressed: ..., label: 'Save', glyph: AppBoxKitGlyphs.save)` — and operations flow through the fluent `action(name, () => ...).withLoading(...).withSnackbars(...)` API (the builder is awaitable — awaiting it executes the operation).

All transient UI goes through `AppBoxKitNotificationService` — the kit-rendered verbs mean apps never register stacked dialog/sheet variants for the generic cases: `show(...)` (toast/snackbar, platform-routed), `confirm(title, ...)` → `bool` (cancel/barrier → false), `prompt(title, ...)` → trimmed `String?` (cancel/empty → null), `alert(title, ...)` (single action), `notice(title, message)` (modal sheet via `appBoxKitShowNativeSheet`). Dialogs/sheets render through `appBoxKitShowNativeDialog`/`appBoxKitShowNativeSheet` (frosted iOS tier, M3 elsewhere); AppBoxKitAction's `dialog`/`bottomSheet` notification types route through `alert`/`notice` too. Tests use `FakeAppBoxKitNotificationService` (records every verb; `confirmResult`/`promptResult` script the answers).

Repo-level consumer map: `ui-library-usage-appendix.md` in this package (SSOT) catalogues every barrel symbol and where it is used across the host app, the tests, the showcase app, and the other kit packages.


### Integration map

<Table id="appbox_kit_ui_library-integration" columns={["Stacked framework deps", "Backing SDK packages", "Depends on (kit)"]} rows={[["stacked, stacked_services, stacked_shared", "get, rxdart, talker_flutter, flutter_animate, shared_preferences, flutter_markdown_plus, flutter_highlighting, flutter_svg, markdown, cupertino_native_better, text_field_m3e, m3e_collection", "appbox_kit_core, appbox_kit_motion"]]} />

See `kit_matrix.md` for the full routing + dependency graph.


### Architecture

`appbox_kit_ui_library` sits in the single tier above core — its only kit edge is `appbox_kit_ui_library → appbox_kit_core`, which it re-exports so consumers need one import. The `Kit*` widgets are adaptive **ports**: each takes primitives (labels, SF Symbol names, `AppBoxKitGlyph` tokens) and delegates to a vendored fork for the native render — `AppBoxKitNativeButton`/`AppBoxKitNativeAppBar` route to `cupertino_native_better` (real Liquid Glass on iOS/macOS 26+, Cupertino below, Material on Android/web), while `AppBoxKitNativeFab`, `AppBoxKitNativeNavigationRail`, `AppBoxKitNativeSplitButton`, `AppBoxKitNativeTextField`, and `AppBoxKitNativeToolbar` delegate to their `*_m3e` forks. The UI-coupled services (`AppBoxKitNavigationControllerService`, `AppBoxKitBottomSheetService`, `AppBoxKitNotificationService`) are drop-in stacked `StackedService` subclasses resolved through `appBoxKitLocator`, and `AppBoxKitAction` is a static facade composing managers (error/loading/notification/stream/success) with executors behind a fluent builder. No widget or service reaches for a plugin type directly — native tiers live behind the port, so the kit ships genuine native chrome with no native code of its own.


### Gotchas

Native setup lives in the host, not the kit: Android predictive back requires `enableOnBackInvokedCallback=true` in the manifest, and every `cupertino_native_better` hosted view must opt out of safe-area regions (`UIHostingController` platform views otherwise compress on fling). On iOS the notification tier is fire-and-forget `CNToast` by default — passing an `actionLabel` is the *only* thing that promotes it to a snackbar, so never assume a snackbar renders on iOS. `AppBoxKitBottomSheetService` preserves the stacked API but silently ignores Get-specific knobs (`enableDrag`, enter/exit durations, `useRootNavigator`, `elevation`), and `isBottomSheetOpen` still reads Get state and will report `false` for kit-presented sheets. Maintenance risk is concentrated in `vendor/` — the six forks (`cupertino_native_better`, `fab_m3e`, `navigation_rail_m3e`, `split_button_m3e`, `text_field_m3e`, `toolbar_m3e`) carry their own tests and must be re-verified on iOS SDK bumps; stacked 3.5.0's `AdaptivePage` is material-on-all-platforms, which is exactly why `AppBoxKitPlatformPagesMixin` exists.

AppBoxKitAction ops are owner-keyed via the `AppBoxKitActionOwner` mixin (`AppBoxKitViewModel`, `AppBoxKitDataFacade`, adapters/services `with AppBoxKitActionOwner`) — keys derive as `RuntimeType#identityHash.name` (`AppBoxKitAction.deriveKey`), never hand-written. **The hub is the app-level API**: `abxActionHub.on<P, R>('name', (payload) => ..., errorMessage:, withValue:, errorNotification:, successNotification:, loadingNotification:, *NotificationType:, onError:, onSuccess:, onSend:, debounce:, throttle:, parallelExecution:, flushOnDispose:, retry:, timeout:, confirmTitle:, confirmMessage:, confirmActionLabel:, confirmDestructive:)` registers one hot command channel per op, and `command.send(payload)` ALWAYS executes — it returns `Future<R>`, an *observation handle* on the already-running op (async_redux dispatch/dispatchAndWait model): awaiting is optional, dropping is harmless (handle errors are pre-observed, never unhandled). A send while the same key is in flight never re-runs — its handle completes with the in-flight run's result, never a guard error (`parallelExecution: true` opts out: every send runs, each handle gets its own result). Debounce-superseded handles complete with the eventual run's result (debounce drops events, not callers); throttle is leading-edge with the window anchored at the last run's START (throttled-away handles observe the in-flight/previous run). `errorMessage` is completeOnError parity (swallow + `withValue` fallback; without it the handle completes with the original error), `onError`/`onSuccess` are the handleError/onSuccess taps (onSuccess awaited after the op, before the success notification and handle completion; tap errors only warn), `loadingNotification` fires at run start, and the notification params route to snackbar (default) or the kit-rendered alert/notice surfaces (dialog/bottomSheet types). `confirmTitle:` gates the run on AppBoxKitNotificationService.confirm — a decline never executes the op (no busy state, no notifications) and the handle completes with `withValue`. Dispose cancels subs then subjects; pending debounced sends complete with a disposed error UNLESS the command set `flushOnDispose: true`, which executes them immediately (attaching to an in-flight run per guard semantics) — `dispose()` returns a `Future` that awaits those flushes, and in-flight ops always run to completion: cancellation is cooperative-only, Dart futures cannot be aborted. The `pipeline` getter is lazy (override `createPipeline()` for owner-wide defaults) and dies with `disposeAppBoxKitActions()` — which `AppBoxKitViewModel.dispose` calls, so no manual wiring. Need real mid-flight cancel, `toStream`, or progress callbacks? The **low-level builder** (`action(name, () => ...)` / `AppBoxKitAction.run`) is LAZY (an un-awaited chain never runs — exactly the footgun pipes exist to kill) and stays in kit only for those forms; app code should not use it — note its `withProgress` is dead config (no executor ever invokes it), there is no progress concept to port. Op busy/error state is a stream either way: `vm.actionState$('name')` (pipe and builder share the derived key and the state manager) bound in views via `AppBoxKitStreamBuilder`. `watch(name, streams:, callback:)` takes any `Stream` and its callback receives the emitted value — it is for VM-internal side effects, never view data (views bind streams directly); compose dependent subscriptions with rxdart `switchMap` into one stream instead of nesting listeners. Any code path that can hit AppBoxKitAction's error handling needs `AppBoxKitErrorService.initialize()` called at boot first (late Talker), and widget-less tests must register `FakeAppBoxKitNotificationService` under the explicit `AppBoxKitNotificationService` type or the real CNToast path dies on the missing navigator context.

Liquid-glass surfaces *inside a scrollable under pinned chrome* ghost on iOS: the compositor's native clip chain is the only thing occluding a `UiKitView` during scroll and it is historically unreliable (flutter #25965/#76097/#154664; `BackdropFilter` glass joins the artifact class via #175048). Wrap any `CN*`-backed widget or `AppBoxKitGlassCard` that lives in a `CustomScrollView` in `AppBoxKitScrollOcclusionGate` (or the `.scrollOcclusion()` extension) — proportional fade to alpha 0 detaches the native layer by full coverage; `AppBoxKitNativeChromeGate` is the *modal-depth* sibling and composes with it. Reviewer check 1i enforces the pairing; full mechanism in `core/NATIVE_COMPONENTS.md` ("Scrolled under pinned chrome").

The bottom-edge scrim is a `LinearGradient` with per-stop alpha, never an `Opacity` and never a `BackdropFilter` band — a blur band cannot sample platform-view pixels (they punch through) and an alpha wrapper is the forbidden mutator over native glass, so the gradient is the only lawful way to keep scrolled content from hard-clipping at the physical bottom edge. Toasts mount on `Overlay.maybeOf(ctx, rootOverlay: true)` so the pill outranks every surface in the app — a non-root overlay puts it *under* sheets and dialogs. Chrome hiding routes through `AppBoxKitNativeChromeGate`: `AppBoxKitChromeHideMode.keepAlive` is the default and hides at *paint* level (the child stays mounted behind an `IndexedStack` whose index selects an empty placeholder, so layout, state, and semantics survive), with `unmount` as the explicit opt-out. Prefer the gate over hand-rolled conditional chrome — see `docs/liquid-glass-allowlist.md` for which surfaces travel with a transition.


### Testing

Tests never touch platform channels — services are exercised by hand-written recording stubs registered into `appBoxKitLocator` before the call. The canonical pattern (see `test/kit/services/appbox_kit_notification_service_test.dart`) is a `_RecordingSnackbarService` whose `showCustomSnackBar` override increments `calls` and captures `lastMessage`, `lastVariant`, `lastDuration`, and `lastActionLabel`; assertions then check `expect(svc.calls, 1)` plus the mapped variant and forwarded fields. Platform branches are forced with `AppBoxKitPlatform.override` (e.g. `AppBoxKitPlatformOverride(isAndroid: true)`) so the Android-vs-iOS tier split is deterministic on the macOS test host. The `CNToast` tier is fire-and-forget and paint-unasserted — its test asserts the *negative* (`SnackbarService` NOT invoked) to lock the de-mix contract rather than the toast pixels.


### Features

<Checklist id="appbox_kit_ui_library-features" items={[{"id": "appbox_kit_ui_library-feat-0", "label": "`AppBoxKitNativeComponent` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-1", "label": "`AppBoxKitButtonType` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-2", "label": "`AppBoxKitButtonLoaderDuration` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-3", "label": "`AppBoxKitButtonLoaderState` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-4", "label": "`AppBoxKitIconButtonShape` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-5", "label": "`AppBoxKitAnimatedNavbarContainerHeightState` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-6", "label": "`AppBoxKitBlockPanelExpanded` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-7", "label": "`AppBoxKitBlockPanelAlignment` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-8", "label": "`AppBoxKitContainerExpandDirection` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-9", "label": "`AppBoxKitContainerExpansionLimit` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-10", "label": "`AppBoxKitContainerRetractButtonPosition` \u2014 enum"}, {"id": "appbox_kit_ui_library-feat-11", "label": "`AppBoxKitAction` \u2014 class"}]} />


### Decisions / ADRs

`appbox_kit_ui_library` is the one kit permitted to depend on `appbox_kit_core` — the matrix's star topology makes `kit → core` the only allowed kit edge (discouraged for capability kits but required here because the UI tier is genuinely MVVM-coupled). It is deliberately **not** standalone: it owns the Stacked-coupled surface (router pages, `BottomSheetService`/`SnackbarService` subclasses, appBoxKitLocator resolution) that capability kits are kept free of. The six vendored forks were folded in (ADR-0002, relocate-vendor) rather than consumed from pub.dev because each carries a fix upstream ignores — `navigation_rail_m3e` repairs the unclipped expand overflow and indicator-corner sync, `cupertino_native_better` is the only path to real Liquid Glass on iOS 26+, and `AppBoxKitPlatformPagesMixin` exists because stacked 3.5.0's `AdaptivePage` builds a material route on every platform and never attaches the iOS edge-swipe gesture. Vendoring trades a maintenance debt (re-verify on iOS SDK bumps) for genuine native chrome with no native code in the kit itself.

The 2026-08-13/14 glass rulings changed three defaults: edge treatment is on for both edges rather than opt-in, the dialog/input-bar/card glass surfaces paint an opaque base rather than a transparent one, and chrome hiding is a paint-level gate that keeps the child mounted rather than a widget-tree removal. Rationale for all three lives in `docs/liquid-glass-allowlist.md` — THE liquid-glass law, and the SSOT for *why*; this playbook only indexes the resulting API.

<!-- kb:begin -->
### References

Official docs and tooling:

- [Stacked — official documentation](https://stacked.filledstacks.com) — Canonical framework docs. Start here for ViewModel/Service patterns used by every kit.
- [stacked (pub.dev)](https://pub.dev/packages/stacked) — Lightweight MVVM + service appBoxKitLocator. Pinned ^3.5.0 across the kits.
- [stacked_shared (pub.dev)](https://pub.dev/packages/stacked_shared) — StackedLocator.instance — the shared singleton kits resolve through. Pinned ^1.4.2.
- [stacked_generator (pub.dev)](https://pub.dev/packages/stacked_generator) — Generates routers + @StackedApp wiring. Pinned ^2.0.4.
- [stacked_cli (pub.dev)](https://pub.dev/packages/stacked_cli) — Scaffolding CLI for new Stacked apps/views/services.
- [Flutter — official documentation](https://docs.flutter.dev) — Framework docs, platform integration, performance.
- [Dart — official documentation](https://dart.dev) — Language tour, async, package ecosystem.
- [Dart & Flutter MCP server (official)](https://dart.dev/tools/mcp-server) — Ships with the Dart SDK (dart mcp-server). Exposes analyzer diagnostics, pub, tests, and live Flutter app introspection via DTD. Wired in appbox_kit/.mcp.json.

Kit-specific:

- [Flutter API reference](https://api.flutter.dev) — Full widget/framework API. The source of truth for Flutter classes.
- [Flutter cookbook](https://docs.flutter.dev/cookbook) — Recipes for common Flutter tasks.
- [get (pub.dev)](https://pub.dev/packages/get) — get — backing dependency of appbox_kit_ui_library.
- [rxdart (pub.dev)](https://pub.dev/packages/rxdart) — rxdart — backing dependency of appbox_kit_ui_library.
- [talker_flutter (pub.dev)](https://pub.dev/packages/talker_flutter) — talker_flutter — backing dependency of appbox_kit_ui_library.
- [flutter_animate (pub.dev)](https://pub.dev/packages/flutter_animate) — flutter_animate — backing dependency of appbox_kit_ui_library.
- [shared_preferences (pub.dev)](https://pub.dev/packages/shared_preferences) — shared_preferences — backing dependency of appbox_kit_ui_library.
- [flutter_markdown_plus (pub.dev)](https://pub.dev/packages/flutter_markdown_plus) — flutter_markdown_plus — backing dependency of appbox_kit_ui_library.
- [flutter_highlighting (pub.dev)](https://pub.dev/packages/flutter_highlighting) — flutter_highlighting — backing dependency of appbox_kit_ui_library.
- [flutter_svg (pub.dev)](https://pub.dev/packages/flutter_svg) — flutter_svg — backing dependency of appbox_kit_ui_library.
- [markdown (pub.dev)](https://pub.dev/packages/markdown) — markdown — backing dependency of appbox_kit_ui_library.
- [cupertino_native_better (pub.dev)](https://pub.dev/packages/cupertino_native_better) — cupertino_native_better — backing dependency of appbox_kit_ui_library.
- [text_field_m3e (pub.dev)](https://pub.dev/packages/text_field_m3e) — text_field_m3e — backing dependency of appbox_kit_ui_library.
- [m3e_collection (pub.dev)](https://pub.dev/packages/m3e_collection) — m3e_collection — backing dependency of appbox_kit_ui_library.
<!-- kb:end -->

<QuestionForm id="open-questions" questions={[
  {
    id: "q-appbox_kit_ui_library-gaps",
    title: "What is unclear or missing from this playbook?",
    mode: "freeform",
    placeholder: "Note anything a new contributor or agent would need that is not covered above.",
  },
]} />

