# split_button_m3e — vendored fork playbook

**Version:** 0.2.1 (fork) · **Upstream:** `EmilyMoonstone/material_3_expressive` · **Status:** vendored fork, pinned by root `dependency_overrides`

**Role (one line):** Material 3 Expressive split button (two segments: leading action + trailing menu trigger) — forked so the dropdown is positioned edge-aware from the trailing segment's real rect instead of snapping to the screen's left edge.

<Callout id="split-fork-banner" tone="warning">

Vendored fork of upstream `split_button_m3e` `0.2.1`. The patched source lives in this repo at `packages/split_button_m3e/lib/src/split_button.dart`. The patch is activated by the root `pubspec.yaml` `dependency_overrides` `path:` entry — remove that entry and `flutter pub get` silently reverts to upstream.

</Callout>

## Contents

0. Fork banner
1. Overview & role
2. Glossary
3. Architecture — fork contract + FileTree
4. Public API reference
5. Usage playbook
5b. Wiring
6. Integration map
7. Features appendix
8. Examples
9. Gotchas + re-sync procedure
10. Decisions / ADRs
11. Changelog & status
12. Open questions

### Overview & role

`split_button_m3e` is a leaf Material 3 Expressive UI widget package. Hexagonal / domain-layer concerns are N/A — it has no data, service, or routing surface. It renders a two-segment button and consumes `m3e_design` tokens.

This package is one of four vendored forks of `EmilyMoonstone/material_3_expressive` pinned in this repo (alongside `fab_m3e`, `navigation_rail_m3e`, `toolbar_m3e`). It is the only one of the four that ships **both** a `test/` and an `example/` directory.

### What this is

A vendored fork of upstream `split_button_m3e` `0.2.1`. The fork is pulled in transitively via the `m3e_collection` umbrella package, and activated by a `path:` `dependency_overrides` entry in the root `pubspec.yaml`. The patched file is `packages/split_button_m3e/lib/src/split_button.dart` (the `_menuPosition()` method). The patch marker in source is `// Patched (vendored fork)`.

### Glossary

- **Fork** — a vendored copy of an upstream package, edited in-tree, pinned by a `path:` override.
- **Override** — the `dependency_overrides:` block in the root `pubspec.yaml` that forces `pub` to resolve this package from a local folder instead of pub.dev.
- **Patch marker** — an inline comment tag (`// Patched (vendored fork)`, `// Fork patch:`, or `// PATCH`) that flags every block of code changed from upstream. Audit with `grep -rEn 'FORK|fork patch|PATCH|vendored' packages/split_button_m3e/lib`.
- **Re-sync** — the procedure for re-applying this fork's patches onto a newer upstream version (see §9).
- **M3E** — Material 3 Expressive, the motion/shape system this widget implements.
- **Trailing segment** — the chevron half of the split button; the menu trigger. Has its own `GlobalKey` (`_trailingKey`) so the fork can read its real `RenderBox` rect.

### Architecture — the fork contract

The fork contract has two halves: **WHY** the patch exists (the root `pubspec.yaml` override comment) and **HOW** it is implemented (the inline `// Patched (vendored fork)` marker in `lib/src/split_button.dart`).

<Diagram id="split-arch-diagram" caption="split_button_m3e fork wiring: root override → umbrella → fork package.">

```html
<div class="diagram-panel" data-rough>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">root pubspec.yaml</div>
    <div class="diagram-node">dependency_overrides</div>
  </div>
  <div class="diagram-arrow">↓ re-exported by</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">m3e_collection</div>
    <div class="diagram-node">umbrella re-export</div>
  </div>
  <div class="diagram-arrow">↓ path: override redirects</div>
  <div class="diagram-box diagram-accent" data-rough>
    <div class="diagram-pill">split_button_m3e (fork)</div>
    <div class="diagram-node">lib/src/split_button.dart _menuPosition()</div>
  </div>
  <div class="diagram-arrow">↓ wrapped by</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">appbox_kit</div>
    <div class="diagram-node">Kit* wrappers</div>
  </div>
</div>
```

```css
.diagram-panel { display: flex; flex-direction: column; gap: 8px; }
.diagram-arrow { text-align: center; padding: 2px 0; opacity: .7; }
```

</Diagram>

<FileTree id="split-file-tree" title="split_button_m3e — files that matter for the fork" entries={[
  { path: "packages/split_button_m3e/lib/split_button_m3e.dart", change: "modified", note: "Barrel; exports src/enums.dart, src/menu_items.dart, src/split_button.dart." },
  { path: "packages/split_button_m3e/lib/src/split_button.dart", change: "modified", note: "PATCHED — _menuPosition() computes edge-aware RelativeRect from the trailing segment's real RenderBox rect. Single // Patched (vendored fork) marker block; supporting comments document showMenu's private _PopupMenuRouteLayout coupling." },
  { path: "packages/split_button_m3e/lib/src/enums.dart", change: "modified", note: "SplitButtonM3EShape/Size/Emphasis/TrailingAlignment + SplitButtonM3ETokens. Unpatched." },
  { path: "packages/split_button_m3e/lib/src/menu_items.dart", change: "modified", note: "SplitButtonM3EItem<T> model. Unpatched." },
  { path: "packages/split_button_m3e/test/split_button_m3e_test.dart", change: "modified", note: "test/ exists." },
  { path: "packages/split_button_m3e/example/", change: "modified", note: "example/ exists — only fork of the four with both test+example." },
  { path: "packages/split_button_m3e/CHANGELOG.md", change: "modified", note: "STALE — only 0.1.0 entry; 0.2.1 fork patch not documented." },
  { path: "packages/split_button_m3e/pubspec.yaml", change: "modified", note: "issue_tracker has EmilyMonestone (sic) typo." },
]} />

### The fork contract (shared)

Every vendored fork in this repo carries the same contract:

- The **WHY** is documented in the root `pubspec.yaml` `dependency_overrides` comment block above each `path:` entry. Read it before editing.
- The **HOW** is flagged in source with `// Patched (vendored fork)`, `// Fork patch:`, or `// PATCH`. Every changed block has a marker.
- The **audit command** is `grep -rEn 'FORK|fork patch|PATCH|vendored' packages/<name>/lib` — re-run after every edit and before every re-sync.
- The **doc-gap checklist** (every fork carries this debt): add a `> Vendored fork of upstream <name> <version>` banner to the README; backfill the CHANGELOG; fix the `EmilyMonestone` → `EmilyMoonstone` typo in `issue_tracker` (present in every fork's `pubspec.yaml`).
- The **test contract**: every fork SHOULD ship a `test/`. `navigation_rail_m3e` currently has NONE (highest debt); the other three forks do ship one.

### Public API reference

The barrel `lib/split_button_m3e.dart` exports three files. `SplitButtonM3E` is generic on the menu value type `<T>`. The fork only changes the `_menuPosition()` private method; the public signatures are unchanged from upstream.

<TabsBlock id="split-api-tabs" tabs={[
  {
    id: "split-api-button",
    label: "SplitButtonM3E<T>",
    blocks: [
      {
        id: "split-api-button-code",
        type: "annotated-code",
        data: {
          filename: "packages/split_button_m3e/lib/src/split_button.dart",
          language: "dart",
          code: "class SplitButtonM3E<T> extends StatefulWidget {\n  const SplitButtonM3E({\n    super.key,\n    this.shape = SplitButtonM3EShape.round,\n    this.size = SplitButtonM3ESize.sm,\n    this.emphasis = SplitButtonM3EEmphasis.filled,\n    this.label,\n    this.leadingIcon,\n    this.onPressed,\n    required this.items,\n    this.onSelected,\n    this.trailingAlignment = SplitButtonM3ETrailingAlignment.opticalCenter,\n    this.leadingTooltip,\n    this.trailingTooltip,\n    this.enabled = true,\n    this.menuBuilder,\n  });\n\n  final SplitButtonM3EShape shape;\n  final SplitButtonM3ESize size;\n  final SplitButtonM3EEmphasis emphasis;\n  final String? label;\n  final IconData? leadingIcon;\n  final VoidCallback? onPressed;\n  final List<SplitButtonM3EItem<T>>? items;\n  final List<PopupMenuEntry<T>> Function(BuildContext)? menuBuilder;\n  final ValueChanged<T>? onSelected;\n  final SplitButtonM3ETrailingAlignment trailingAlignment;\n  final bool enabled;\n}",
          annotations: [
            { lines: "1", label: "Generic", note: "SplitButtonM3E<T> — T is the menu value type. Items are SplitButtonM3EItem<T>." },
            { lines: "3-5", label: "shape / size / emphasis", note: "Tokens-driven; pressed morph uses tokens. Unchanged from upstream." },
            { lines: "11", label: "items xor menuBuilder", note: "Provide a static list (items) OR a builder that returns PopupMenuEntry<T>. Asserted at construction." },
            { lines: "16", label: "trailingAlignment", note: "Chevron alignment strategy (e.g. opticalCenter). Unchanged from upstream." },
          ],
        },
      },
    ],
  },
  {
    id: "split-api-item",
    label: "SplitButtonM3EItem<T>",
    blocks: [
      {
        id: "split-api-item-code",
        type: "annotated-code",
        data: {
          filename: "packages/split_button_m3e/lib/src/menu_items.dart",
          language: "dart",
          code: "class SplitButtonM3EItem<T> {\n  const SplitButtonM3EItem({\n    required this.value,\n    required this.child,\n    this.enabled = true,\n  });\n\n  final T value;\n  final Object child; // Widget or plain string; the caller builds PopupMenuItem\n  final bool enabled;\n}",
          annotations: [
            { lines: "7", label: "child", note: "Object — Widget or plain string. The fork's _openMenu builds the PopupMenuItem and styles disabled children at 0.38 alpha." },
          ],
        },
      },
    ],
  },
  {
    id: "split-api-enums",
    label: "Enums",
    blocks: [
      {
        id: "split-api-enums-text",
        type: "rich-text",
        data: {
          markdown: "**`SplitButtonM3EShape`** — `round` / `square` (resting outer shape; pressed morph uses tokens).  \n**`SplitButtonM3ESize`** — XS / S / SM / M / L / XL.  \n**`SplitButtonM3EEmphasis`** — `filled` / `tonal` / `outlined` / `elevated`.  \n**`SplitButtonM3ETrailingAlignment`** — chevron alignment (e.g. `opticalCenter`).  \n\nAll unchanged from upstream.",
        },
      },
    ],
  },
  {
    id: "split-api-menuposition",
    label: "_menuPosition (the patch)",
    blocks: [
      {
        id: "split-api-menuposition-code",
        type: "annotated-code",
        data: {
          filename: "packages/split_button_m3e/lib/src/split_button.dart",
          language: "dart",
          code: "RelativeRect _menuPosition(BuildContext context) {\n  final RenderBox overlay =\n      Overlay.of(context).context.findRenderObject() as RenderBox;\n  final BuildContext? tCtx = _trailingKey.currentContext;\n  RenderBox? targetBox = tCtx?.findRenderObject() as RenderBox?;\n  targetBox ??= context.findRenderObject() as RenderBox?;\n  // ... targetRect from targetBox.localToGlobal + size\n\n  const double _kMenuVerticalOffset = 8.0;\n  const double _kMenuScreenPadding = 8.0;\n  final EdgeInsets safe = MediaQuery.paddingOf(Overlay.of(context).context);\n  final double estHeight =\n      (widget.items?.length ?? 0) * kMinInteractiveDimension + 16.0;\n  final double fitBottom = overlay.size.height - _kMenuScreenPadding - safe.bottom;\n  final bool below =\n      targetRect.bottom + _kMenuVerticalOffset + estHeight <= fitBottom;\n  final double top = below\n      ? targetRect.bottom + _kMenuVerticalOffset\n      : (targetRect.top - estHeight - _kMenuVerticalOffset);\n  // ... _menuMaxHeight side-effect\n\n  // Patched (vendored fork): pass the true trailing rect so showMenu is edge-aware.\n  final double left = targetRect.left;\n  final double right = overlay.size.width - targetRect.right;\n  return RelativeRect.fromLTRB(left, top, right, overlay.size.height);\n}",
          annotations: [
            { lines: "2-3", label: "overlay context", note: "Read MediaQuery.paddingOf from Overlay.of(context).context — that's where showMenu reads it. A Scaffold body can consume padding; reading from the button's context would under-count the bottom nav/safe-area." },
            { lines: "4-6", label: "trailing rect", note: "Prefer the trailing segment's RenderBox (_trailingKey); fall back to the whole control. This is the rect that anchors the menu." },
            { lines: "9", label: "8px gap", note: "Was 4 upstream. 8 matches KitNativePopupMenu so both dropdowns sit the same distance below their trigger." },
            { lines: "13-15", label: "flip above", note: "If the menu does not fit below (using the same screen-fit bound showMenu uses), flip it above the trigger." },
            { lines: "22-24", label: "edge-aware left/right", note: "THE PATCH. Upstream pinned left=0.0 in LTR so _PopupMenuRouteLayout always picked the left side. Passing the real rect lets showMenu right-align for right-of-centre triggers and left-align for left-of-centre." },
          ],
        },
      },
    ],
  },
]} />

### Usage playbook

<AnnotatedCode id="split-usage-basic" filename="example/split_usage.dart" language="dart" code={"import 'package:m3e_collection/m3e_collection.dart';\n\nSplitButtonM3E<String>(\n  label: 'Send',\n  leadingIcon: Icons.send,\n  onPressed: () => _send(),\n  onSelected: (v) => _sendAs(v),\n  items: [\n    SplitButtonM3EItem(value: 'plain', child: 'Plain'),\n    SplitButtonM3EItem(value: 'encrypted', child: 'Encrypted'),\n  ],\n)"} annotations={[
  { lines: "1", note: "Hosts import via the umbrella m3e_collection, not the fork directly (see 5b)." },
  { lines: "2", label: "generic", note: "SplitButtonM3E<String> — T flows through items, onSelected, and the PopupMenuEntry list." },
  { lines: "7-10", label: "items", note: "SplitButtonM3EItem.child accepts a Widget or a plain string; the fork's _openMenu builds the PopupMenuItem and applies disabled-alpha styling." },
]} />

## 5b. Wiring — how a host actually gets this fork

<AnnotatedCode id="split-wiring-override" filename="pubspec.yaml (root)" language="yaml" code={"# Vendored fork of split_button_m3e 0.2.1 (pulled in via m3e_collection).\n# Patched so the dropdown is positioned edge-aware from the trigger's real rect\n# (right-aligned for a right-side trigger, left for a left one) instead of\n# snapping to the screen's left edge. Patch: packages/split_button_m3e/lib/src/\n# split_button.dart _menuPosition.\ndependency_overrides:\n  split_button_m3e:\n    path: packages/split_button_m3e"} annotations={[
  { lines: "1-4", label: "WHY", note: "Root pubspec.yaml override comment. This is the auditable reason for the fork; edit it whenever the patch changes." },
  { lines: "5-7", label: "HOW", note: "path: override activates the fork. m3e_collection still re-exports split_button_m3e by name; the override redirects that name to the local folder." },
]} />

<Callout id="split-wiring-callout" tone="info">

Hosts almost never import `package:split_button_m3e/...` directly. The fork is pulled in **transitively** via `m3e_collection` (the umbrella that re-exports every M3E widget), and most apps reach it through `appbox_kit`'s `Kit*` wrappers (e.g. `KitNativePopupMenu`). The root `dependency_overrides` `path:` entry is the only thing that activates the patch — `m3e_collection`'s own version constraint is unchanged.

</Callout>

### Integration map

<Diagram id="split-integration-map" caption="Apps import m3e_collection; the root override redirects split_button_m3e to the local fork.">

```html
<div class="diagram-panel" data-rough>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">App (showcase / host)</div>
  </div>
  <div class="diagram-arrow">↓ imports</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">m3e_collection</div>
    <div class="diagram-node">umbrella</div>
  </div>
  <div class="diagram-arrow">↓ re-exports</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">appbox_kit</div>
    <div class="diagram-node">KitNativePopupMenu + Kit* wrappers</div>
  </div>
  <div class="diagram-arrow">↓ fork activated by override</div>
  <div class="diagram-box" data-rough>
    <div class="diagram-pill">root pubspec.yaml</div>
    <div class="diagram-node">dependency_overrides: split_button_m3e path:packages/split_button_m3e</div>
  </div>
  <div class="diagram-arrow">↓ redirects to local folder</div>
  <div class="diagram-box diagram-accent" data-rough>
    <div class="diagram-pill">packages/split_button_m3e</div>
    <div class="diagram-node">this fork, not pub.dev</div>
  </div>
</div>
```

```css
.diagram-panel { display: flex; flex-direction: column; gap: 8px; }
.diagram-arrow { text-align: center; padding: 2px 0; opacity: .7; }
```

</Diagram>

### Features appendix

<Checklist id="split-features" items={[
  { id: "feat-two-segment", label: "Two-segment split button (leading action + trailing menu)" },
  { id: "feat-sizes", label: "Six size rows (XS-XL)" },
  { id: "feat-emphasis", label: "Four emphasis families (filled / tonal / outlined / elevated)" },
  { id: "feat-shapes", label: "Round or square resting shape; pressed morph via tokens" },
  { id: "feat-items-or-builder", label: "items: list OR menuBuilder: for dynamic PopupMenuEntry lists" },
  { id: "feat-edge-aware", label: "Edge-aware dropdown positioning from trailing rect (fork)", checked: true, note: "Right-aligned for right-of-centre triggers, left for left-of-centre. Passes the true rect to showMenu so _PopupMenuRouteLayout picks the right side." },
  { id: "feat-8px-gap", label: "8px vertical gap (parity with KitNativePopupMenu) (fork)", checked: true, note: "Was 4 upstream; 4 read as 'touching' the chevron." },
  { id: "feat-flip-above", label: "Flips above when no fit below (fork)", checked: true, note: "Uses the same screen-fit bound showMenu uses (screen - 8 - safeArea.bottom)." },
  { id: "feat-max-height-cap", label: "Caps _menuMaxHeight to free space (fork)", checked: true, note: "Set as a side-effect in _menuPosition; read by _openMenu's showMenu constraints. Overlap-proof even when estHeight is wrong." },
  { id: "feat-a11y", label: "48x48 min tap targets per segment" },
  { id: "feat-chevron", label: "Caret rotation on open via AnimatedRotation" },
]} />

### Examples

<Code id="split-example-menubuilder" filename="example/dynamic_menu.dart" language="dart" caption="Build the menu dynamically (e.g. from a ViewModel) instead of a static items list." code={"SplitButtonM3E<MailAction>(\n  label: 'Move',\n  leadingIcon: Icons.move_to_inbox,\n  onPressed: () => _move(),\n  menuBuilder: (ctx) => _folders\n      .map((f) => PopupMenuItem(value: MailAction(f.id), child: Text(f.name)))\n      .toList(),\n  onSelected: (a) => _moveTo(a),\n)"} />

### Gotchas + re-sync procedure

<Callout id="split-gotcha-private-api" tone="risk">

The patch couples to **Flutter's private `_PopupMenuRouteLayout`** behaviour — specifically that it compares `position.left` vs `position.right` to pick which screen edge to anchor against. This is not a stable public API. On any Flutter SDK bump, re-test that the menu still anchors to the trailing edge for centre and right-side triggers. If `_PopupMenuRouteLayout` changes its side-picking rule, this patch's edge-aware logic must be re-derived.

</Callout>

<Callout id="split-gotcha-context" tone="warning">

`_menuPosition` reads `MediaQuery.paddingOf(Overlay.of(context).context)` — the **overlay's** context, NOT the button's. A `Scaffold` body consumes bottom padding (e.g. nav-bar insets); reading from the button's context under-counts and lets `showMenu`'s clamp drag the menu back over the chevron. If you refactor the call site, preserve the overlay-context lookup.

</Callout>

<Callout id="split-gotcha-sideeffect" tone="warning">

`_menuPosition` is **not pure** — it sets `_menuMaxHeight` as a side-effect, which `_openMenu` then reads when calling `showMenu(constraints: BoxConstraints(maxHeight: _menuMaxHeight))`. If you cache or reorder the call, the cap is lost and a tall menu can be clamped back over the trigger.

</Callout>

### Re-sync procedure (shared across all four forks)

When bumping upstream (`fab_m3e`, `navigation_rail_m3e`, `split_button_m3e`, or `toolbar_m3e`):

1. **Bump** the fork's `pubspec.yaml` `version:` and the root `pubspec.yaml` override comment's stated version.
2. **Backfill** the fork's `CHANGELOG.md` with the new version entry (every fork's CHANGELOG is stale — see §11).
3. **Diff** each patched file against the new upstream release of the same path.
4. **Re-apply** each `// Patched (vendored fork)` / `// Fork patch:` / `// PATCH` block by hand. Do NOT copy upstream over the fork blindly — the markers are your only map of what to preserve.
5. **Update** the root `pubspec.yaml` override comment if the WHY or the patched-file list changed.
6. **Run** the fork's `flutter test` in `packages/<name>/`, then run the consuming showcase (`appbox_kit_showcase`) to confirm the patched behaviour still renders.

### Override hygiene (shared)

<Callout id="split-override-hygiene" tone="warning">

Never remove a `dependency_overrides` `path:` entry without first confirming upstream includes the fix. A "clean" `flutter pub get` silently reverts the fork to the pub.dev release, and the patch — including its bug fix — disappears with no compile error. The root override comment is the only durable record of why the patch exists.

</Callout>

<Callout id="split-doc-gap" tone="risk">

Doc-gap debt every fork carries: (a) no `> Vendored fork of upstream <name> <version>` banner in the README; (b) stale `CHANGELOG.md`; (c) `EmilyMonestone` → `EmilyMoonstone` typo in `issue_tracker` (present in every fork's `pubspec.yaml`).

</Callout>

### Decisions / ADRs

None. The fork contract is documented in the root `pubspec.yaml` override comment; no separate ADR was written. The private-API coupling (`_PopupMenuRouteLayout`) is the strongest argument for an ADR if the patch surface grows.

### Changelog & status

The fork's `CHANGELOG.md` is **stale** — it only contains the `0.1.0` initial-release entry. There is no `0.2.1` entry documenting the fork patch.

**Backfill task:** add a `## 0.2.1` entry describing the edge-aware `_menuPosition`, the 8px gap (parity with `KitNativePopupMenu`), the flip-above fit test, the `_menuMaxHeight` cap, the overlay-context `MediaQuery.paddingOf` lookup, and the `_PopupMenuRouteLayout` side-picking coupling.

**Test status:** `test/split_button_m3e_test.dart` exists. The `example/` directory also exists — this is the only fork of the four with both.

### Open questions

<QuestionForm id="split-open-questions" questions={[
  {
    id: "split-q-private-api",
    title: "How do we detect a _PopupMenuRouteLayout regression on Flutter SDK bumps?",
    subtitle: "The patch couples to a private Flutter layout class.",
    mode: "single",
    options: [
      { id: "golden", label: "Golden test of the menu anchored centre + right", detail: "Catches a side-pick regression visually.", recommended: true },
      { id: "manual", label: "Manual showcase smoke per Flutter bump", detail: "Cheap; easy to forget." },
      { id: "integration", label: "Drive showMenu in an integration test and assert rect", detail: "Most rigorous; highest setup cost." },
    ],
    required: false,
  },
  {
    id: "split-q-upstream-pr",
    title: "Should the edge-aware positioning be upstreamed as a PR?",
    mode: "freeform",
    placeholder: "Note any blockers (e.g. upstream wants a different API surface).",
  },
]} submitLabel="Record answers" />
