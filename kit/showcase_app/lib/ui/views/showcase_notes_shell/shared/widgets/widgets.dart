/// Barrel for shell-wide shared widgets under `showcase_notes_shell/shared/widgets/`.
/// Views in this shell import this barrel — never a sibling view's widgets
/// (kit folder-org gate check C/D).
library;

export 'auth_text_field.dart';
export 'form_error_row.dart';
