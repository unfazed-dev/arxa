import 'package:stacked/stacked.dart';

/// The components demos are stateless (every action is an imperative kit
/// call) — the viewmodel exists to satisfy the showcase's 5-file surface
/// pattern.
class ShowcaseComponentsViewModel extends BaseViewModel {}
