<Callout id="header" tone="info">

# arxa_kit_support — Per-Package Playbook


`arxa_kit_support` v**0.1.0** · `publish_to: 'none'` · status: **active**


**Role (one line):** In-app support port for arxa_kit apps — screenshot+annotation feedback flow (feedback package) bundled with an injected Talker log export, routed to a swappable submission sink (local-file export by default).


**Scope of this document:** lets a human OR an LLM use, wire, and extend `arxa_kit_support` **without reading its source**. The public surface below was extracted from the barrel; README content is folded into the narrative sections.


## Table of contents

0. Header & TOC — *you are here* · 1. Overview & role · 2. Public surface · 3. Usage & wiring · 4. Integration map · 5. Architecture · 6. Gotchas · 7. Testing · 8. Features · 9. Decisions · 10. References · 11. Open questions


</Callout>


### Overview & role

In-app support port for arxa_kit apps — screenshot+annotation feedback flow (feedback package) bundled with an injected Talker log export, routed to a swappable submission sink (local-file export by default). Standalone; no arxa_kit core dependency.


### Public surface

Exported from `lib/arxa_kit_support.dart`:

<Table id="support-api" columns={["Symbol", "Kind", "Defined in"]} rows={[["ArxaKitSupportContextBuilder", "typedef", "src/arxa_kit_support_service.dart"], ["ArxaKitSupportService", "class", "src/arxa_kit_support_service.dart"], ["ArxaKitDiagnosticsBundle", "class", "src/arxa_kit_support_types.dart"], ["ArxaKitFeedbackSubmission", "class", "src/arxa_kit_support_types.dart"], ["ArxaKitSubmissionResult", "class", "src/arxa_kit_support_types.dart"], ["ArxaKitEmailSubmissionSink", "class", "src/sinks/arxa_kit_email_submission_sink.dart"], ["ArxaKitGithubIssueSink", "class", "src/sinks/arxa_kit_github_issue_sink.dart"], ["ArxaKitSubmissionSink", "abstract class", "src/sinks/arxa_kit_submission_sink.dart"], ["ArxaKitCallbackSubmissionSink", "class", "src/sinks/arxa_kit_submission_sink.dart"], ["ArxaKitLocalFileSubmissionSink", "class", "src/sinks/arxa_kit_submission_sink.dart"]]} />

Test doubles in `lib/arxa_kit_testing.dart`: `RecordingArxaKitSubmissionSink`.

Scripts and call-counts — tests never touch platform channels.


### Usage & wiring

- Wrap the app in **`BetterFeedback`** at the root (from the `feedback`
  package) — `startFeedbackFlow` requires that ancestor.
- `feedback`'s screenshots need Flutter's **CanvasKit renderer** on web, and
  platform views (maps, webviews) render blank in the screenshot.
- `ArxaKitLocalFileSubmissionSink` uses `dart:io`; on web, inject a
  `ArxaKitCallbackSubmissionSink` instead.


### Integration map

<Table id="support-integration" columns={["Stacked framework deps", "Backing SDK packages", "Depends on (kit)"]} rows={[["\u2014", "feedback, talker_flutter, path_provider", "\u2014 (standalone)"]]} />

See `kit_matrix.md` for the full routing + dependency graph.


### Architecture

`ArxaKitSupportService` is the single entry point and depends only on two things it is handed: the app's already-registered `Talker` instance (injected, never constructed here) and a `ArxaKitSubmissionSink`. `ArxaKitSubmissionSink` is the port — an abstract `submit(ArxaKitFeedbackSubmission)` — that the concrete adapters (`ArxaKitLocalFileSubmissionSink`, `ArxaKitCallbackSubmissionSink`, plus the `ArxaKitEmailSubmissionSink` / `ArxaKitGithubIssueSink` stubs) implement, so the service is transport-agnostic. Dependency direction runs one way: service → port → the immutable types in `arxa_kit_support_types.dart` (`ArxaKitDiagnosticsBundle`, `ArxaKitFeedbackSubmission`, `ArxaKitSubmissionResult`), and the sinks depend on those same types rather than back on the service. No edge points at `arxa_kit` core, `stacked`, or `stacked_services`.


### Gotchas

`startFeedbackFlow` resolves `BetterFeedback.of(context)`, so the host MUST wrap the app root in `feedback`'s `BetterFeedback` widget or it throws. `feedback`'s screenshots require Flutter's CanvasKit renderer on web, and platform views (maps, webviews) render blank in the captured PNG. `ArxaKitLocalFileSubmissionSink` uses `dart:io`, so on web inject a `ArxaKitCallbackSubmissionSink` instead; the `ArxaKitEmailSubmissionSink` and `ArxaKitGithubIssueSink` are deliberate stubs that throw `UnimplementedError` — wire a real transport before relying on them. Because the `Talker` is injected, passing a fresh/empty instance ships a diagnostics bundle with no history — hand the service the same singleton the app logs to.


### Testing

`import 'package:arxa_kit_support/arxa_kit_testing.dart';` for
`RecordingArxaKitSubmissionSink` (records `submissions`, scriptable `result`),
`arxaKitFakeFeedbackSubmission(...)`, and `arxaKitCannedDiagnosticsBundle(...)` — assert the
diagnostics/submit path without a widget tree or a real Talker.


### Features

<Checklist id="support-features" items={[{"id": "support-feat-0", "label": "`ArxaKitSupportContextBuilder` \u2014 typedef"}, {"id": "support-feat-1", "label": "`ArxaKitSupportService` \u2014 class"}, {"id": "support-feat-2", "label": "`ArxaKitDiagnosticsBundle` \u2014 class"}, {"id": "support-feat-3", "label": "`ArxaKitFeedbackSubmission` \u2014 class"}, {"id": "support-feat-4", "label": "`ArxaKitSubmissionResult` \u2014 class"}, {"id": "support-feat-5", "label": "`ArxaKitEmailSubmissionSink` \u2014 class"}, {"id": "support-feat-6", "label": "`ArxaKitGithubIssueSink` \u2014 class"}, {"id": "support-feat-7", "label": "`ArxaKitSubmissionSink` \u2014 abstract class"}, {"id": "support-feat-8", "label": "`ArxaKitCallbackSubmissionSink` \u2014 class"}, {"id": "support-feat-9", "label": "`ArxaKitLocalFileSubmissionSink` \u2014 class"}]} />


### Decisions / ADRs

The package backs onto `feedback` for the screenshot+annotation flow (its overlay and editor are mature and self-contained) and onto `talker_flutter` for diagnostics precisely because arxa_kit apps already register a `Talker` as a lazy singleton — reusing that instance avoids a competing logger and ships the same log history the app has been writing. It is intentionally standalone (no `arxa_kit` core, `stacked`, or `stacked_services` edge) so a host can adopt in-app support without pulling the rest of the kit, and it is not a crash reporter — Crashlytics/Sentry stay a separate concern. The default sink is `ArxaKitLocalFileSubmissionSink` (app-documents files) because it carries no transport dependency; the email and github-issue sinks ship as stubs so the package never pulls an SMTP/HTTP client until a host explicitly opts in. The `ArxaKitSubmissionSink` port exists so a host can swap in Supabase, an existing HTTP client, or a web-friendly callback without forking the service.

<!-- kb:begin -->
### References

Official docs and tooling:

- [Stacked — official documentation](https://stacked.filledstacks.com) — Canonical framework docs. Start here for ViewModel/Service patterns used by every kit.
- [stacked (pub.dev)](https://pub.dev/packages/stacked) — Lightweight MVVM + service locator. Pinned ^3.5.0 across the kits.
- [stacked_shared (pub.dev)](https://pub.dev/packages/stacked_shared) — StackedLocator.instance — the shared singleton kits resolve through. Pinned ^1.4.2.
- [stacked_generator (pub.dev)](https://pub.dev/packages/stacked_generator) — Generates routers + @StackedApp wiring. Pinned ^2.0.4.
- [stacked_cli (pub.dev)](https://pub.dev/packages/stacked_cli) — Scaffolding CLI for new Stacked apps/views/services.
- [Flutter — official documentation](https://docs.flutter.dev) — Framework docs, platform integration, performance.
- [Dart — official documentation](https://dart.dev) — Language tour, async, package ecosystem.
- [Dart & Flutter MCP server (official)](https://dart.dev/tools/mcp-server) — Ships with the Dart SDK (dart mcp-server). Exposes analyzer diagnostics, pub, tests, and live Flutter app introspection via DTD. Wired in arxa_kit/.mcp.json.

Kit-specific:

- [feedback (pub.dev)](https://pub.dev/packages/feedback) — feedback — backing dependency of arxa_kit_support.
- [talker_flutter (pub.dev)](https://pub.dev/packages/talker_flutter) — talker_flutter — backing dependency of arxa_kit_support.
- [path_provider (pub.dev)](https://pub.dev/packages/path_provider) — path_provider — backing dependency of arxa_kit_support.
<!-- kb:end -->

<QuestionForm id="open-questions" questions={[
  {
    id: "q-support-gaps",
    title: "What is unclear or missing from this playbook?",
    mode: "freeform",
    placeholder: "Note anything a new contributor or agent would need that is not covered above.",
  },
]} />

